# 거래 취소/환급 지출 상쇄 오류 분석 및 수정 계획서 (`error_v1.md`)

이 문서는 **가족 가계부 및 자산관리 프로그램**의 뱅크샐러드 가져오기 및 카테고리 지출 집계 시, **결제 취소/환급 거래(지출 카테고리이나 엑셀 원본 +금액인 항목)**가 상쇄 처리되지 않고 이중 가산되던 원인을 현재 소스 코드 기준으로 분석하고 수정 계획을 정의한 사양서입니다.

---

## 1. 지적된 현상 및 문제점

### 1.1 현상 및 엑셀 원본 부호
- 5월 `생활고정비` 카테고리의 Google Payments Gemini 결제건과 같이, 최초 테스트 결제(`-1,000원`) 후 결제 취소/승인취소(`+1,000원`)가 동시 발생한 경우:
  - **엑셀 원본 금액**: 일반 결제 **`-1,000원`**, 결제 취소 **`+1,000원`**
  - 기대 결과: $+1,000\text{원} - 1,000\text{원} = \mathbf{0\text{원}}$ (지출 상쇄)
  - 기존 결과: $+1,000\text{원} + 1,000\text{원} = \mathbf{2,000\text{원}}$ (이중 지출 가산 오류)

---

## 2. 현재 소스 코드 기준 원인 분석

### 2.1 `src/utils/excelParser.js` (라인 85)
```javascript
// 기존 코드
let numAmount = Math.abs(Number(String(amountRaw).replace(/,/g, '')) || 0);
```
- **원인**: 엑셀 파싱 시 `Math.abs(...)` 함수를 일괄 적용하여 엑셀 원본의 취소/환급 양수 부호(`+1,000원`)와 일반 승인 음수 부호(`-1,000원`)가 모두 절대값(양수 1,000원)으로 강제 변환되어 취소 정보가 소실되었습니다.

### 2.2 `src/utils/finance.js` (라인 139~143)
```javascript
// 기존 코드
if (type === '지출') {
  realConsumption += amt;
  totalCashOutflow += amt;
  categorySpentMap[category] += amt;
}
```
- **원인**: 파싱 단계에서 부호 구분이 없어져 무조건 1,000원으로 넘어오므로, 일반 결제(1,000원)와 결제 취소(1,000원)가 모두 카테고리 지출액에 플러스 가산되어 `1,000 + 1,000 = 2,000원`으로 누적되었습니다.

---

## 3. 수정 로직 및 구현 계획

### 3.1 엑셀 원본 부호 매핑 명세 (지출 카테고리)

사용자께서 명확히 규정해 주신 엑셀 원본 부호 규격에 맞춰 아래와 같이 정규화합니다:

| 거래 구글/가맹점 | 엑셀 원본 금액 (E열) | 거래 유형 (B열) | 지출 카테고리 가산/차감 | 회계 처리 결과 |
|---|---|:---:|---|---|
| **일반 결제 (정상 승인)** | **`-1,000원`** (음수) | `지출` | **`+1,000원` 가산** | 지출 카테고리 금액 **증가** |
| **결제 취소 (승인 취소/환급)** | **`+1,000원`** (양수) | `지출` | **`-1,000원` 차감** | 지출 카테고리 금액 **차감 (상쇄)** |

### 3.2 소스 코드별 수정 상세안

1. **`src/utils/excelParser.js` (파서 수정)**:
   - 엑셀 원본 `E열(금액)`이 음수(`-1,000원`)인 경우: 일반 지출 거래 (지출 금액 `1,000`)
   - 엑셀 원본 `E열(금액)`이 양수(`+1,000원`)이면서 `지출` 유형인 경우: **결제 취소건**으로 판별하여 `isCancel: true` 및 `amount: -1000` (음수 지출) 처리.

2. **`src/utils/finance.js` (집계 유틸리티 수정)**:
   - 카테고리별 사용액 집계 시 결제 취소건(`amount < 0` 또는 `isCancel`)을 차감:
     $$\text{카테고리 지출 사용액} = \sum \text{일반 결제액(+1,000)} + \sum \text{결제 취소액(-1,000)} = \mathbf{0\text{원}}$$

3. **`src/components/TransactionsView.jsx` (UI 및 합계 Row 수정)**:
   - 결제 취소 거래 행에 **`↩️ 결제취소`** 하늘색 배지 표시.
   - 금액 열에 `+1,000원 (환급/취소)` 형태로 표기.
   - 테이블 하단 `<tfoot>` 실시간 지출 합계 행에서도 취소건이 정확히 차감 반영되어 합계가 연동됨.

---

## 4. 사용자 승인 요청

위 **`error_v1.md` 명세(일반 결제 `-1,000원` ➔ 지출 가산, 결제 취소 `+1,000원` ➔ 지출 차감/상쇄)**를 검토해 주시기 바랍니다.

확인 및 승인해 주시면 소스 코드에 바로 반영하도록 하겠습니다!
