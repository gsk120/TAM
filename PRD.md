# 가족 가계부 및 자산관리 프로그램 PRD

> 문서 상태: Draft v1.0  
> 작성 기준: 사용자와의 가계부 관련 대화에서 직접 확인된 요구사항만 우선 반영  
> 작성일: 2026-07-29  
> 통화/지역: 대한민국 원화(KRW), 대한민국 표준시(KST)

---

## 0. 문서 작성 원칙과 검증 기준

이 문서는 사용자가 지금까지 설명한 가계부 운영 방식, 예산, 계좌·카드 구조, 분류 규칙, 자산·부채 관리 요구를 바탕으로 작성한다.

다음 기준을 적용했다.

1. **사용자가 직접 말한 내용**은 `확정 요구사항`으로 반영한다.
2. 기존 대화에서 AI가 먼저 제안했지만 사용자가 명확히 확정하지 않은 내용은 `미확정 또는 향후 검토`로 분리한다.
3. 금액이나 합계는 문서 작성 시 다시 계산해 검증한다.
4. 서로 충돌할 수 있는 과거 규칙은 가장 최근 대화 기준을 우선한다.
5. 프로그램 구현 편의를 위해 필요한 일반적인 기술 기능을 임의로 필수 요구사항에 넣지 않는다.
6. 사용자가 승인하지 않은 외부 금융기관 자동연동, AI 자동분류, 알림, 공동계정 기능 등은 기본 범위에 확정 기능으로 포함하지 않는다.

---

# 1. 제품 개요

## 1.1 제품명

**가족 가계부 및 자산관리 프로그램**

가칭이며 실제 서비스명은 추후 결정한다.

## 1.2 제품 목적

대한민국 4인 가족의 월별 수입과 지출을 관리하고, 예산 초과 여부, 현금흐름, 금융자산 증가, 부채 감소, 순자산 변화를 한곳에서 확인하기 위한 개인용 재무관리 프로그램을 만든다.

단순히 소비금액만 기록하는 가계부가 아니라 다음을 동시에 관리하는 것이 목적이다.

- 월별 수입과 지출
- 카테고리별 예산 및 잔액
- 부부 공동생활비와 개인용돈
- 주택담보대출과 마이너스통장
- 청약, 자녀 투자금, CMA, 주식, 코인, 퇴직연금 등 금융자산
- 부동산과 부채를 포함한 순자산
- 실비보험 환급을 반영한 순의료비
- 자산 이동과 실제 소비의 구분
- 월 잉여자금과 마이너스통장 상환 가능액

## 1.3 해결하려는 문제

현재 사용자는 뱅크샐러드에서 거래내역을 내려받아 Google Sheets 또는 엑셀에 주 1회 반영하고 있다. 이 방식은 유연하지만 다음 문제가 있다.

- 거래내역 정리와 카테고리 수정이 반복적으로 필요하다.
- 카드 사용액과 카드대금 출금이 중복 집계될 수 있다.
- 서울페이, 스타벅스 카드 등 선불수단은 실제 사용내역을 별도로 기록해야 한다.
- 청약, 자녀 투자, 대출 원금상환이 소비인지 자산형성인지 구분하기 어렵다.
- 실비보험금이 다음 달에 입금되면 월별 의료비가 왜곡된다.
- 월 예산, 부채상환, 순자산 변화를 여러 시트에서 따로 관리해야 한다.
- 대출금리 변동 시 월 고정비와 잉여자금을 다시 계산해야 한다.

본 프로그램은 위 작업을 하나의 일관된 규칙으로 관리해야 한다.

---

# 2. 사용자 및 가구 구성

## 2.1 주요 사용자

- 기석
- 승주

## 2.2 가족 구성원

- 기석
- 승주
- 첫째 자녀(서아)
- 둘째 자녀(서빈)

## 2.3 거래 귀속 구분

각 거래는 필요할 경우 다음 중 하나로 귀속할 수 있어야 한다.

- 가족공동
- 기석
- 승주
- 서아
- 서빈

단, 자녀 지출은 독립 예산을 두지 않고 기본적으로 `육아`, `의료비`, 향후 `교육비`에 포함한다.

---

# 3. 핵심 제품 원칙

## 3.1 단순한 입력, 명확한 분석

- 거래 입력은 빠르고 간단해야 한다.
- 카테고리는 지나치게 세분화하지 않는다.
- 애매한 거래를 매번 쪼개지 않고 주된 목적 기준으로 분류한다.
- 프로그램은 사용자가 소비를 통제하고 자산을 늘리는 데 필요한 수준으로 분석한다.

## 3.2 현금흐름과 소비를 구분

같은 거래라도 다음 세 관점이 다를 수 있다.

1. **현금흐름**: 통장에서 실제 돈이 빠져나갔는가
2. **소비**: 재화나 서비스를 소비했는가
3. **자산·부채 변화**: 금융자산이 증가했거나 부채가 감소했는가

예시:

- 청약통장 납입: 현금유출은 있지만 소비가 아니라 자산 이동
- 주담대 원금상환: 현금유출은 있지만 소비가 아니라 부채 감소
- 주담대 이자: 현금유출이면서 금융비용
- 카드대금 출금: 카드 승인 시 이미 소비로 기록했다면 추가 소비가 아님

## 3.3 월 예산과 실제 지출 중심

- 월별 예산 대비 사용액과 남은 금액을 즉시 확인한다.
- 예산은 월별로 변경할 수 있어야 한다.
- 과거 월의 예산값은 보존되어야 한다.
- 기본안, 보수안, 교육비 발생안 등 예산 시나리오를 비교할 수 있어야 한다.

## 3.4 부채상환 중심

월 잉여자금은 단순한 남는 돈이 아니라 마이너스통장 상환 재원으로 본다.

프로그램은 다음을 연결해서 보여줘야 한다.

- 월 수입
- 월 현금유출
- 월 잉여자금
- 마이너스통장 원금상환액
- 월말 마이너스통장 잔액
- 예상 완납 시점

---

# 4. 현재 운영 방식

## 4.1 거래 수집

사용자는 대체로 주 1회 다음 절차로 가계부를 정리한다.

1. 뱅크샐러드에서 거래내역을 엑셀 형태로 내려받는다.
2. 불필요한 거래 또는 잘못 분류된 거래를 정리한다.
3. 가계부의 거래내역에 붙여넣는다.
4. 월별 카테고리 사용액과 잔여예산을 확인한다.
5. 누락된 선불결제, 현금, 지원금 사용내역 등을 수동 입력한다.

## 4.2 현재 가계부 구조

기존 Google Sheets 가계부는 다음 개념을 사용한다.

- 대시보드
- 연간현황
- 총자산
- 수입관리
- 월별한도 또는 월별예산
- 내역

프로그램에서는 위 구조를 그대로 시트 형태로 복제할 필요는 없지만, 동일한 정보를 화면과 데이터로 제공해야 한다.

---

# 5. 제품 범위

## 5.1 필수 범위

1. 거래내역 등록·수정·삭제
2. 엑셀 또는 CSV 거래내역 가져오기
3. 수입·지출·자산이동·부채상환 구분
4. 카테고리 관리
5. 월별 예산 관리
6. 월별 수입과 지출 집계
7. 실비를 반영한 순의료비 계산
8. 주택담보대출과 마이너스통장 관리
9. 총자산, 총부채, 순자산 관리
10. 월간 및 연간 분석
11. 계좌·카드·선불잔액 관리
12. 데이터 내보내기 및 백업

## 5.2 현재 범위에서 제외

- 은행·카드사 계정에 직접 로그인하는 자동수집
- 세금 신고 또는 연말정산 기능
- 투자 매매 주문 기능
- 대출 신청 및 대환 실행 기능
- 보험 청구 실행 기능
- 신용평가 또는 투자상품 추천

---

# 6. 초기 계좌 및 금융수단

## 6.1 현금 계좌 영역

-기업은행(기석)
-하나은행(기석)
-케이뱅크(기석)
-기업은행(승주)
-우리은행(승주)
-부산은행(승주)
-케이뱅크(승주)
-미래에셋CMA
-기업은행중금채
-하나은행적금
-우리은행적금
-주택청약(기석)
-주택청약(승주)
-부산은행예금

## 6.2 투자/자산

토스증권(기석)
-미래에셋(기석)
-업비트(기석)
-농협투자(승주)
-토스증권(승주)
-퇴직연금(기석)
-퇴직연금(승주)
-부동산

## 6.3 카드 및 결제수단

현재 대화에서 확인된 관리 대상은 다음과 같다.

- 승주 우리카드
- 기석 신한카드
- 기석 삼성카드
- 기석 KB금융포인트리카드
- 기석 KB쿠팡카드
- 기석 케이뱅크 체크카드
- 승주 케이뱅크 체크카드
- 기석 하나K패스 체크카드
- 서울페이
- 스타벅스 선불충전
- 첫만남이용권 등 정부지원 바우처

카드별 혜택과 전월실적 자동관리는 본 PRD의 필수 범위가 아니며 향후 검토 기능으로 둔다.

## 6.4 계좌 필드

각 계좌는 다음 정보를 가질 수 있어야 한다.

- 계좌명
- 금융기관
- 계좌종류
- 소유자
- 현재 잔액
- 자산 또는 부채 구분
- 거래 집계 포함 여부
- 메모

## 6.5 계좌종류

- 입출금
- 예금
- 적금
- CMA
- 청약통장
- 증권계좌
- 퇴직연금·IRP
- 자녀 투자계좌
- 회사 출자금
- 주택담보대출
- 마이너스통장
- 기타 자산
- 기타 부채

---

# 7. 거래 데이터 구조

## 7.1 필수 필드

- 거래일자
- 거래유형
- 대분류
- 거래내용 또는 가맹점명
- 거래금액

기존 엑셀 기준으로는 다음 열과 대응한다.

- A열: 날짜
- B열: 유형
- C열: 카테고리
- D열: 내용
- E열: 금액

## 7.2 권장 필드

- 소유자 또는 귀속 구성원
- 계좌
- 카드 또는 결제수단
- 중분류
- 메모
- 태그
- 고정비 여부
- 반복거래 여부
- 실비 청구 가능 여부
- 자산이동 여부
- 수동입력 여부
- 원본 출처
- 원본 행 식별값
- 생성일시
- 수정일시

## 7.3 거래유형

최소 다음 유형을 제공한다.

- 수입
- 지출
- 계좌이체
- 자산증가
- 자산감소
- 부채증가
- 부채상환
- 잔액조정

## 7.4 금액 처리

- 내부 금액은 원 단위 정수로 저장한다.
- 원본 데이터의 지출이 양수 또는 음수여도 거래유형 기준으로 표준화한다.
- **결제 취소 및 환급 지출건 상쇄 처리 (error_v1.md)**: 
  - 엑셀 원본 E열 금액 기준: 일반 결제는 **`-1,000원`**(음수), 결제 취소건은 **`+1,000원`**(양수).
  - 회계 처리: 거래유형이 `지출`인 취소건(`+1,000원`)은 카테고리 지출 합계에서 차감(-/상쇄) 처리하여 `-1,000원 + 1,000원 = 0원`으로 정확하게 상쇄되어야 한다.
- 화면에서는 지출을 음수로 보여줄지 양수로 보여줄지 사용자가 선택할 수 있게 할 수 있으나, 내부 집계 결과는 일관되어야 한다.

---

# 8. 지출 카테고리 및 예산

## 8.1 기본 카테고리

1. 대출
2. 보험
3. 통신비
4. 주거비
5. 생활고정비
6. 금융고정비
7. 교통
8. 식비
9. 육아
10. 의료비
11. 교육비
12. 기타생활비
13. 이벤트
14. 세금
15. 기석용돈
16. 승주용돈

카테고리는 사용자가 추가·수정할 수 있어야 한다.

## 8.2 기본 월예산

| 카테고리 | 기본 월예산 |
|---|---:|
| 대출 | 2,900,000원 |
| 보험 | 550,000원 |
| 통신비 | 140,000원 |
| 주거비 | 210,000원 |
| 생활고정비 | 260,000원 |
| 금융고정비 | 420,000원 |
| 교통 | 150,000원 |
| 식비 | 800,000원 |
| 육아 | 500,000원 |
| 의료비 | 150,000원 |
| 교육비 | 0원 |
| 기타생활비 | 300,000원 |
| 이벤트 | 100,000원 |
| 세금 | 0원 |
| 기석용돈 | 200,000원 |
| 승주용돈 | 200,000원 |

세금 예산을 제외한 기본 월예산 합계는 **6,880,000원**이다.

월 예산은 사용자가 프로그램으로 직접 수정할 수 있어야한다
예산 수정시 모든 월에 일괄 조정되는 것이 아니라, 수정 날의 월부터 적용 되어야한다.

### 8.2.1 기본 월예산 합계 검증

```text
2,900,000
+ 550,000
+ 140,000
+ 210,000
+ 260,000
+ 420,000
+ 150,000
+ 800,000
+ 500,000
+ 150,000
+ 0
+ 300,000
+ 100,000
+ 200,000
+ 200,000
= 6,880,000원
```

## 8.3 보수적 대출 예산안

대출 예산을 3,150,000원으로 상향한 보수안도 저장한다.

- 기본 총예산: 6,780,000원
- 대출 예산 증가분: 250,000원
- 보수안 총예산: **7,030,000원**

안으로 갖고 있고 사용자가 수정할 수 있으면 된다.

## 8.4 향후 교육비 발생안

첫째의 유치원 또는 영어교육 비용이 본격적으로 발생하면 교육비를 월 500,000원 수준으로 별도 설정하는 시나리오를 지원한다.

- 보수안 총예산: 7,030,000원
- 교육비: 500,000원
- 교육비 포함 총예산: **7,530,000원**

교육비 500,000원은 향후 예상값이며 현재 확정 지출이 아니다.

안으로 갖고 있고 사용자가 수정할 수 있으면 된다.

---

# 9. 카테고리별 분류 규칙

## 9.1 대출

### 포함

- 주택담보대출 월 원리금
- 마이너스통장 이자
- 기타 대출이자

### 처리 원칙

주택담보대출 원리금은 월 예산 화면에서 `대출` 카테고리의 현금유출로 전체 금액을 보여준다.

자산·소비 분석에서는 다음과 같이 분리한다.

- 원금: 부채 감소
- 이자: 금융비용 또는 소비성 지출

마이너스통장 원금상환은 일반 생활지출에서 제외하고 `부채상환`으로 표시한다.

## 9.2 보험

### 포함

- 기석 보험
- 승주 보험
- 첫째 보험
- 둘째 보험
- 실손보험
- 운전자보험
- 자동차보험
- 기타 보장성보험

### 초기 월예산

- 550,000원

실비보험금 수령은 보험료의 마이너스 지출로 처리하지 않고 `실비` 수입으로 별도 기록한다.

## 9.3 통신비

### 포함

- 휴대전화 요금
- 인터넷
- IPTV
- 통신 부가서비스

### 초기 월예산

- 140,000원

회사에서 통신비를 사후 환급하는 경우:

- 결제 시점: 통신비 전체를 지출
- 환급 시점: 기타수입

미리 순액으로 줄여 기록하지 않는다.

## 9.4 주거비

### 포함

- 아파트 관리비
- 전기
- 가스
- 수도
- 주거 유지에 필요한 반복 비용

### 초기 월예산

- 210,000원

재산세는 주거비가 아니라 `세금`으로 분류한다.

## 9.5 생활고정비

### 포함

- 정수기 렌탈
- 생활 관련 렌탈
- 차량 커넥트 서비스
- 가족이 사용하는 반복 서비스
- 금융상품이 아닌 정기 생활 구독

### 초기 월예산

- 260,000원

차량 커넥트 서비스 월 5,500원과 같은 소액 반복 결제도 생활고정비에 포함한다.

## 9.6 금융고정비

### 포함

- 기석 청약통장
- 승주 청약통장
- 첫째 자녀 증여·투자금
- 둘째 자녀 증여·투자금
- 정기적인 금융자산 적립

### 초기 월예산

- 420,000원

현재 계획에 반영된 예시는 다음과 같다.

- 기석 청약통장: 월 20,000원
- 승주 청약통장: 월 20,000원
- 첫째 자녀 증여·투자: 월 190,000원
- 둘째 자녀 증여·투자: 월 190,000원
- 합계: 420,000원

### 처리 원칙

- 현금흐름 예산에서는 금융고정비로 포함
- 순수 소비 분석에서는 소비에서 제외
- 총자산 화면에서는 금융자산 증가로 반영

## 9.7 교통

### 포함

- 대중교통
- 택시
- 주유
- 주차
- 통행료
- 차량 관련 소액 유지비

### 초기 월예산

- 150,000원

자동차보험은 교통이 아니라 보험으로 분류한다.

## 9.8 식비

### 포함

- 식료품
- 마트 장보기
- 가족 외식
- 배달음식
- 가족이 함께 이용한 카페

### 초기 월예산

- 800,000원

### 카페 분류

- 가족이 함께 방문: 식비
- 기석 혼자 이용: 기석용돈
- 승주 혼자 이용: 승주용돈

### 혼합 영수증

마트 거래에 식료품과 육아용품이 섞여 있어도 기본적으로 분할하지 않는다.

- 주요 목적이 식료품 또는 일반 장보기: 식비
- 아기용품이 주목적: 육아
- 판단이 어려운 일반 장보기: 식비

## 9.9 육아

### 포함

- 기저귀
- 분유
- 아기용품
- 어린이집 관련 비용
- 자녀 의류
- 장난감
- 자녀 전용 식품
- 자녀 전용 생활용품
- 의료가 아닌 자녀 관련 비용

### 초기 월예산

- 500,000원

병원, 약국, 처방약, 예방접종 등 명확한 의료비는 자녀 지출이라도 의료비로 분류한다.

## 9.10 의료비

### 포함

- 병원
- 약국
- 치과
- 한의원
- 건강검진
- 예방접종
- 처방 의약품
- 병원 처방 또는 병원 판매 아토피 로션
- 가족 전체의 치료 목적 비용

### 초기 월예산

- 150,000원

### 제외

- 일반 영양제
- 건강기능식품
- 애사비
- 치료 목적이 아닌 일반 건강제품

성인용 건강제품은 기타생활비로 처리한다. 자녀 전용 비의료 건강제품은 육아로 처리할 수 있다.

### 실비 반영

월 순의료비는 다음과 같이 계산한다.

```text
순의료비 = MAX(0, 해당 월 의료비 총지출 - 해당 월 실비 수입)
```

화면에는 다음을 모두 보여준다.

- 의료비 총지출
- 실비 수입
- 순의료비

실비가 해당 월 의료비보다 큰 경우 순의료비는 음수가 아니라 0원이다.

실비 초과분의 다음 달 이월 차감은 기본적으로 적용하지 않는다.

## 9.11 교육비

### 포함

- 유치원
- 학원
- 영어교육
- 교재
- 학습지
- 교육 프로그램
- 학교 또는 교육기관 비용

### 현재 월예산

- 0원

### 향후 예상

- 월 500,000원 내외

향후 첫째 교육비가 발생하면 육아와 구분해 별도 카테고리로 관리한다.

## 9.12 기타생활비

### 포함

- 생활잡화
- 의류
- 미용
- 성인 건강기능식품
- 영양제
- 애사비
- 집안 소모품
- 다른 카테고리에 속하지 않는 일반 생활비

### 초기 월예산

- 300,000원

## 9.13 이벤트

### 포함

- 양가 부모님 생신
- 명절
- 가족 경조사
- 가족 기념일
- 친인척 선물

### 초기 월예산

- 100,000원

현재 이벤트 예산은 다음 연간 추정을 월평균으로 나눈 값이다.

- 양가 부모님 생신: 4명 × 200,000원 = 800,000원
- 명절 등: 연 400,000원
- 연간 합계: 1,200,000원
- 월평균: 100,000원

실제 지출이 없는 달에도 연간 행사 준비금 관점으로 월 100,000원을 예산에 둘 수 있다.

## 9.14 세금

### 포함

- 재산세
- 자동차세
- 주민세
- 종합부동산세
- 기타 지방세
- 생활과 관련된 기타 세금

### 처리 원칙

- 재산세는 주거비가 아니라 세금으로 분류한다.
- 취득세처럼 일회성 대규모 세금은 세금 카테고리에 별도 태그를 부여할 수 있다.

### 예산 방식

세금은 납부월에만 실제 지출 반영한다.
예산은 0원으로 잡고 지출만 기록하는 방식으로 한다.

예산이 0원이라 지출이 항상 초과하기에 예산 상태는 항상 안정으로 둔다.

## 9.15 기석용돈

### 포함

- 기석 개인 커피
- 기석 개인 식사
- 기석 개인 쇼핑
- 기석 개인 취미
- 가족 공동 목적이 아닌 개인 지출

### 초기 월예산

- 200,000원

## 9.16 승주용돈

### 포함

- 승주 개인 커피
- 승주 개인 식사
- 승주 개인 쇼핑
- 승주 개인 취미
- 가족 공동 목적이 아닌 개인 지출

### 초기 월예산

- 200,000원

---

# 10. 수입 카테고리

## 10.1 기본 수입 카테고리

- 기석월급
- 기석상여
- 승주월급
- 승주상여
- 실비
- 기타수입

## 10.2 수입 분류 규칙

- 기석 정규급여: 기석월급
- 기석 상여: 기석상여
- 기석 지연 지급 시간외수당: 기석월급
- 승주 정규급여: 승주월급
- 승주 육아휴직급여: 승주월급
- 부모급여: 단순화를 위해 승주월급으로 포함
- 승주 상여: 승주상여
- 실손보험 환급: 실비
- 회사 통신비 환급: 기타수입
- 부모 또는 조부모가 자녀에게 준 돈을 부모가 생활비에 사용하는 경우: 기타수입
- 금융상품 이자 또는 배당: 기타수입 또는 별도 태그

## 10.3 보수적 월수입 기준

현재 가계부 예산은 실제 수입이 더 많을 수 있어도 보수적으로 다음 값을 기준으로 관리한다.

- 기석 월평균 수입: 7,000,000원
- 승주 육아휴직 중 월평균 수입: 3,000,000원
- 가족 월수입 기준: 10,000,000원

실제 수입과 예산 기준 수입을 분리해서 볼 수 있어야 한다.

---

# 11. 특수거래 처리 규칙

## 11.1 계좌 간 이체

다음은 수입이나 지출이 아니다.

- 월급통장 → 생활비통장
- 생활비통장 → CMA
- CMA → 증권계좌
- 기석 계좌 → 승주 계좌
- 승주 계좌 → 기석 계좌
- 카드 결제계좌로 자금 이동

거래유형은 `계좌이체`로 처리하고 총수입 및 총지출에서 제외한다.

## 11.2 카드 승인과 카드대금 출금

카드 승인 시점에 실제 소비 카테고리로 지출을 기록한다.

이후 결제일에 계좌에서 카드대금이 출금되면 추가 지출로 잡지 않는다.

예시:

- 7월 5일 카드로 식비 100,000원 사용 → 식비 100,000원
- 8월 14일 카드대금 100,000원 출금 → 계좌이체 또는 카드대금 결제

총소비는 100,000원만 반영해야 한다.

## 11.3 서울페이

서울페이 충전은 소비가 아니라 자산 이동이다.

예시:

- 현금 475,000원 결제
- 서울페이 잔액 500,000원 충전
- 충전 혜택 25,000원 발생

처리 방식:

- 475,000원: 현금계좌에서 서울페이 잔액으로 자산 이동
- 25,000원: 기타수입 또는 충전혜택
- 실제 사용 시: 액면 사용금액을 해당 소비 카테고리로 기록

충전과 사용을 모두 소비로 기록하여 중복 집계하지 않는다.

## 11.4 스타벅스 등 선불카드

스타벅스 카드 충전은 자산 이동으로 처리한다.

- 충전 시 지출 아님
- 실제 사용 시 지출

사용 목적에 따른 분류:

- 가족 함께 이용: 식비
- 기석 개인 이용: 기석용돈
- 승주 개인 이용: 승주용돈

뱅크샐러드에서 실제 사용내역이 누락되는 경우 수동 입력한다.

## 11.5 정부지원 바우처

첫만남이용권 등 정부지원 바우처는 현금계좌와 구분해 관리한다.

기본 원칙:

- 지급 시 일반 현금수입으로 합산하지 않음
- 사용 시 실제 소비 목적에 따라 지출 기록
- 결제수단 또는 태그에 바우처명을 표시

예시:

- 분유: 육아
- 아기 병원비: 의료비
- 아기용품: 육아

## 11.6 실비보험

- 병원비 결제: 의료비 지출
- 보험금 입금: 실비 수입
- 월 대시보드: 총의료비, 실비, 순의료비를 분리 표시

실비 수령월이 병원비 지출월과 다르면 월별 순의료비가 달라질 수 있다. 현재 운영 규칙은 각 월의 실제 입출금 기준이며, 순의료비는 0원 미만으로 표시하지 않는다.

## 11.7 청약통장

- 납입 시 현금흐름 예산에는 금융고정비로 포함
- 소비 분석에서는 제외
- 자산 화면에서는 청약통장 잔액 증가로 반영
- 해지 시 원금은 수입이 아니라 자산 이동
- 이자만 수입

## 11.8 회사 출자금 또는 사내 금융자산

- 급여에서 공제된 출자금은 소비가 아니라 자산 증가
- 인출 원금은 수입이 아니라 자산 이동
- 이자 또는 배당만 수입

## 11.9 회사 사후 환급

비용을 먼저 개인이 결제하고 회사가 나중에 환급하면 다음과 같이 처리한다.

- 결제일: 해당 지출 카테고리 전체 금액
- 환급일: 기타수입

최초 지출을 환급 예정액만큼 미리 줄이지 않는다.

## 11.10 가족에게 받은 현금

부모 또는 조부모가 자녀에게 준 돈을 실제로 부모가 생활비 등에 사용하는 경우 기타수입으로 기록한다.

자녀 명의 계좌에 그대로 보관하거나 투자하면 자녀 금융자산 증가로 처리할 수 있다.

## 11.11 혼합 영수증

한 영수증을 기본적으로 여러 거래로 쪼개지 않는다.

- 주된 구매 목적
- 금액 비중이 가장 큰 항목
- 가족이 인식하기 쉬운 기준

중 하나로 단일 카테고리를 선택한다.

필요한 경우에만 거래분할 기능을 사용한다.

---

# 12. 거래내역 가져오기

## 12.1 지원 파일

- Excel
- CSV

## 12.2 필수 기능

- 업로드 파일 미리보기
- 날짜 열 선택
- 유형 열 선택
- 카테고리 열 선택
- 내용 열 선택
- 금액 열 선택
- 계좌·카드 열 선택
- 수입·지출 부호 정규화
- 오류 행 표시
- 업로드 전 최종 확인
- 전체 취소

## 12.3 중복거래 방지

최소 다음 항목을 조합해 중복 가능성을 탐지한다.

- 거래일자
- 금액
- 거래내용
- 계좌 또는 카드

중복 의심 거래는 자동 삭제하지 않고 사용자에게 확인시킨다.

## 12.4 수동 입력

다음 거래는 수동 입력이 가능해야 한다.

- 서울페이 실제 사용
- 스타벅스 선불카드 실제 사용
- 현금거래
- 바우처 사용
- 뱅크샐러드 누락거래
- 잔액조정

---

# 13. 거래내역 관리 기능

## 13.1 목록 기능

- 월별 조회
- 날짜순 정렬
- 금액순 정렬
- 카테고리 필터
- 거래유형 필터
- 소유자 필터
- 계좌 필터
- 카드·결제수단 필터
- 검색

## 13.2 수정 기능

- 거래 수정
- 거래 삭제
- 카테고리 변경
- 귀속 구성원 변경
- 계좌·카드 변경
- 메모 추가
- 태그 추가
- 거래 분할
- 일괄 수정

## 13.3 거래분할

혼합 영수증을 예외적으로 나눌 때 사용한다.

분할 후 금액 합계는 원거래 금액과 같아야 한다.

## 13.4 기 업로드 내역 일괄 삭제 기능

- 엑셀/CSV 업로드 중 잘못된 내역이 있거나 재작업이 필요한 경우, 당월 거래 전체 또는 선택한 가져오기 회차/전체 거래를 한 번에 삭제(Wipe out)할 수 있는 일괄 삭제 기능 제공.
- 삭제 전 사용자 재확인 팝업(Confirmation Modal)을 거친다.

## 13.5 엑셀 스타일 컬럼별 필터링 기능

- 거래 내역 테이블 헤더(날짜, 타입, 대분류, 내용, 귀속, 비고)에 엑셀과 동일한 드롭다운 필터 기능 제공.
- 분석 목적에 맞게 복수의 필터 조건(예: 특정 날짜 범위 + 의료비 + 승주 귀속 + 보험받음)을 동적으로 조합하여 조회가 가능해야 함.

## 13.6 테이블 하단 실시간 필터 합계 행 (Total Summary Row)

- 거래 내역 테이블 맨 아래에 현재 화면에 필터링되어 표시된 거래 항목들의 합계 행을 고정 표기.
- 표기 항목: 총 거래 건수, 지출 합계, 수입 합계, 최종 순합계.
- 필터 조건이 바뀔 때마다 실시간으로 합산 금액이 즉시 재계산됨.

---

# 14. 월별 예산 관리

## 14.1 월별 예산

각 월마다 카테고리별 예산을 저장한다.

예산 변경 시 이전 월의 값은 유지되어야 한다.

예시:

- 2026년 8월 대출 예산: 2,900,000원
- 2026년 9월 이후 대출 예산: 3,150,000원

## 14.2 예산 시나리오

최소 다음 시나리오를 저장·비교할 수 있어야 한다.

- 기본안
- 금리상승 보수안
- 교육비 발생안
- 사용자가 직접 만든 시나리오

## 14.3 예산 계산

- 사용액 = 해당 월 카테고리 지출 합계
- 잔여예산 = 예산 - 사용액
- 사용률 = 사용액 ÷ 예산 × 100

예산이 0원인 카테고리는 사용률 계산 오류가 발생하지 않아야 한다.

---

# 15. 대출관리

## 15.1 주택담보대출

### 현재 기준 입력값

- 대출원금: 510,000,000원
- 상환기간: 30년
- 상환방식: 원리금균등상환
- 실행 예정일: 2026-08-31
- 금리유형: 고정 또는 변동 가능
- 최종 금리: 실행 시점까지 미확정

### 필요한 필드

- 대출명
- 금융기관
- 최초 원금
- 현재 잔액
- 실행일
- 만기일
- 상환방식
- 현재 금리
- 기준금리
- 가산금리
- 우대금리
- 고정기간
- 금리변경주기
- 다음 금리변경일
- 월 원리금
- 월 원금
- 월 이자

### 기능

- 원리금균등 월 납입액 계산
- 월별 원금·이자 분리
- 잔액 계산
- 금리 변경 시 남은 기간 기준 재계산
- 실제 납입액 입력 및 예상값 비교
- 중도상환 또는 추가상환 입력

## 15.2 마이너스통장

### 현재 기준 입력값

- 금융기관: 하나은행
- 예상 사용금액: 90,000,000원
- 현재 가정 금리: 연 4.3%
- 금리변동주기: 6개월

### 기능

- 사용한도와 실제 사용액 구분
- 월 이자 계산
- 월별 금리 변경 이력
- 추가사용액 입력
- 원금상환액 입력
- 월말 잔액
- 월별 상환 추이

### 마통 원금상환액 계산

```text
전월 말 잔액
- 당월 말 잔액
+ 당월 추가사용액
- 이자 및 수수료 조정액
```

거래자료를 통해 정확한 원금상환액을 직접 합산할 수 있으면 직접 합산값을 우선한다.

## 15.3 대출 예산 시나리오 검증

### 기본안

주담대 연 4.5% 가정 시 월 원리금은 약 2,584,095원이며, 마통 이자를 포함해 월 대출 예산 2,900,000원을 사용했다.

### 보수안

주담대 5.0%, 마통 4.3%, 마통 사용액 90,000,000원을 가정하면:

- 주담대 월 원리금: 약 2,737,790원
- 마통 월 이자: 322,500원
- 합계: 약 3,060,290원

금리 변동과 오차에 대비해 월 대출 예산을 **3,150,000원**으로 설정한다.

---

# 16. 자산 및 부채 관리 (총 자산)

## 16.1 개요 및 메뉴 명칭
- 기존 "계좌 & 선불수단" 메뉴 명칭을 **"총 자산"**으로 변경한다.
- 선불수단(서울페이, 스타벅스, 정부 바우처 등) 수기 잔액 관리 기능은 관리 편의를 위해 완전 삭제한다.

## 16.2 월별 자산 수기 입력 관리 원칙 (월간 독립성)
- **월별 독립 수기 관리**: 각 월의 자산/부채 잔액 데이터는 이전 월 데이터가 자동 이월/승계되지 않고 완전 독립적인 월별 데이터로 관리된다. (예: 1월에 20,000원을 입력했더라도 2월에 입력하지 않았다면 이전 월 값이 이월되지 않고 초기값 0원으로 표출됨)
- **수기 입력 구조**: 사용자가 해당 월에 직접 입력한 수치만 해당 월의 자산 데이터로 사용된다. (월중 사전 입력 및 언제든 수정 가능)
- **용어 표기 제약**: 개발 설계용 내부 지칭인 '스냅샷'이라는 단어는 화면 UI의 글로 표기하지 않으며, "월말 자산 현황", "자산/부채 잔액" 등의 자연스러운 표현으로 제공한다.

## 16.3 자산 및 부채 표 구성 (3개 세부 표)

### 1) 현금 계좌 (14개 항목)
- 기업은행(기석), 하나은행(기석), 케이뱅크(기석), 기업은행(승주), 우리은행(승주), 부산은행(승주), 케이뱅크(승주), 미래에셋CMA, 기업은행중금채, 하나은행적금, 우리은행적금, 주택청약(기석), 주택청약(승주), 부산은행예금
- **하단 합계 행**: `현금 합계` = $\sum \text{현금 계좌 항목 잔액}$

### 2) 투자/자산 (8개 항목)
- 토스증권(기석), 미래에셋(기석), 업비트(기석), 농협투자(승주), 토스증권(승주), 퇴직연금(기석), 퇴직연금(승주), 부동산
- **하단 요약 행**:
  - `투자 합계`: $\sum \text{투자/자산 항목 잔액}$
  - `자산 합계`: `현금 합계 + 투자 합계`
  - `투자비중`: `(투자 합계 - 부동산) / 자산 합계` (금융자산 비중 %)

### 3) 부채 (2개 항목)
- 주담대, 마통
- **하단 요약 행**:
  - `부채 합계`: $\sum \text{부채 항목 잔액}$
  - `순자산`: `자산 합계 - 부채 합계`

## 16.4 월별 자산 추이 표 및 시각화 차트

### 1) 연간 월별 자산 추이 요약 표
- 컬럼 구성: `조회월(1월~12월)`, `자산합계`, `부채합계`, `순자산`, `투자비중`

### 2) 시각화 차트 (2개)
- **차트 1**: `자산합계 / 부채합계 / 순자산 추이` (선/막대 차트)
- **차트 2**: `투자비중 추이` (선 차트, %)

## 16.5 당월 자산 데이터 전체 삭제/초기화 기능
- **당월 초기화 버튼**: "총 자산" 화면 상단에 **`🗑️ 당월 자산 초기화`** 버튼을 제공한다.
- **안전 확인 팝업**: 실수로 인한 잔액 소실을 방지하기 위해 사용자 확인 팝업 모달을 거친다. (예: "2026년 7월의 모든 자산 및 부채 입력 데이터가 0으로 초기화됩니다. 계속하시겠습니까?")
- **동작 방식**: 사용자 확인 시 해당 선택월(`selectedMonth`)의 24개 자산/부채 수기 입력 금액이 즉시 `0원`으로 일괄 리셋되며 타 월 데이터에는 영향을 미치지 않는다.

## 16.6 자산 입력 인풋 박스 UX 개선 (포커스 시 덮어쓰기 / 플레이스홀더)
- **포커스 시 지우기 동작 없이 즉시 입력 (Type-over UX)**:
  - 인풋 박스 클릭/포커스(`onFocus`) 시 기존 값을 흐린 백그라운드 글씨(`placeholder`)로 보여주거나 입력 텍스트 전체 선택(`select()`) 처리를 적용한다.
  - 사용자가 백스페이스로 기존 숫자를 지우지 않고 새 금액을 즉시 입력할 수 있도록 지원한다.
- **0원 / 빈값 처리**: 값이 `0`이거나 수정 중인 상태에서는 `placeholder="0"` (흐린 텍스트)로 안내하여 한 번에 깔끔하게 새 금액을 입력하도록 한다.

## 16.7 총 자산 대시보드 시각화 차트 2종 UI/UX 디자인 개편 명세

### 1) 디자인 테마 및 스타일 가이드
- **테마**: 핀테크 프리미엄 다크 모드 (High Contrast Modern Dark Theme)
- **컬러 팔레트**:
  - 자산합계: 시안/하늘색 (Cyan Blue `#38bdf8`)
  - 부채합계: 보조 장미색/레드 (Rose Red `#f43f5e`)
  - 순자산: 네온 에메랄드 연두 (Neon Emerald `#10b981` / `#34d399`)
  - 금융 투자비중: 네온 보라/라벤더 (Neon Purple `#a855f7` / `#c084fc`)
  - 선택월 포인트: 밝은 흰색 Glow 및 네온 툴팁 하이라이트

### 2) 왼쪽 차트: 자산/부채/순자산 추이 혼합형 차트 (Combo Chart)
- **혼합형(Combo) 구성**:
  - `자산합계` & `부채합계`: 1월~12월 이중 막대 (Side-by-side Dual Bars)
  - `순자산`: 막대 그래프 위에 네온 연두 꺾은선 (Glowing Line with Data Nodes)으로 레이어링하여 순자산 변화 추이를 최우선 강조
- **Y축 스케일 최적화**: 0 기준 기본 정렬 및 데이터 변동성이 직관적으로 드러나도록 높이 범위 최적화

### 3) 오른쪽 차트: 금융 투자비중 추이 (%) 영역형 꺾은선 차트 (Gradient Area Line Chart)
- **영역형 꺾은선 형태**: 데이터 포인트를 잇는 깔끔한 꺾은선과 하단 그라데이션 영역 채우기 (Gradient Area Fill)
- **Y축 Auto Scaling (자동 스케일 조정)**:
  - 0%~100% 고정이 아닌 연간 데이터 최댓값(예: 6%)에 맞춰 0%~10% 수준으로 자동 조정하여 미세한 투자비중 변화 폭을 크게 시각화
- **포인트 하라이트 & 툴팁**: 현재 조회월(`selectedMonth`) 노드에 네온 Glow와 툴팁 핀을 배치하여 가시성 극대화

### 4) 차트 데이터 영역과 X축 월 레이블 레이아웃 분리 (글자 겹침 방지)
- **독립적인 플롯 바닥선(Plot Baseline) 설정**:
  - 차트의 0점 베이스라인(`yBase`)을 하단 X축 월 레이블 텍스트 위치보다 최소 **35px 이상 상단**에 배치한다.
- **오버랩 방지**:
  - 0원/0.0%의 꺾은선, 노드 포인트, 비율 텍스트 및 툴팁 뱃지가 "1월~12월" 글자 위로 덮치거나 시각적으로 포개지는 현상을 근본적으로 차단한다.

### 5) 차트 인터랙션 및 현재 월 네온 하이라이트 강화
- **마우스 호버(Hover) 동적 툴팁 인터랙션**:
  - 상시 노출 툴팁 방식을 지양하고, 두 차트 요소에 마우스 포인터를 마우스 호버(`onMouseEnter`/`onMouseLeave`) 했을 때 해당 항목과 수치가 직관적인 네온 툴팁으로 떠오르도록 구현한다.
  - **좌측 차트**: 자산 막대(시안), 부채 막대(장미), 순자산 노드(에메랄드)에 마우스를 올리면 각 항목명과 금액(원)이 동적 툴팁으로 표출됨.
  - **우측 차트**: 각 월 꺾은선 노드에 마우스를 올리면 해당 월과 정확한 금융 투자비중(%)이 툴팁으로 표출됨.
- **우측 차트 현재 월(selectedMonth) 네온 강조 극대화**:
  - 현재 선택된 조회월의 포인터 노드를 다중 네온 링 펄스(Neon Multi-ring Glow & Pulsing Border `#c084fc` / `#e879f9`) 및 선명한 뱃지로 강화하여 눈에 확실히 띄도록 개선한다.

### 6) 호버 툴팁 위치 스마트 플로팅 배치 (그래프 데이터 근접 위치)
- **호버 요소 직상단 스마트 플로팅 (Floating Smart Tooltip)**:
  - 차트 모서리에 고정되어 뜨던 어색한 위치 방식을 배제하고, 마우스를 올린 **막대 그래프 직상단 및 꺾은선 노드 바로 위(X, Y 좌표에 동적 밀착)**에 플로팅 툴팁 뱃지를 렌더링한다.
- **시각적 일체감**:
  - 마우스 포인터 및 그래프 요소 바로 옆/상단에 해당 요소 색상(시안, 장미, 에메랄드, 네온 핑크)의 미세 Glow 툴팁이 바로 따라붙어 직관적인 사용자 경험을 선사한다.

### 7) 꺾은선 노드 X축 정밀 동기화 (1월~12월 범례 1:1 정렬 버그 수정)
- **원인 분석**:
  - 기존 HTML 막대 및 월 레이블 컨테이너의 `justify-content: space-between` 배치가 "1월"을 0% 좌측 끝, "12월"을 100% 우측 끝으로 밀어내어, SVG 꺾은선 노드 중심 좌표`((i + 0.5) * (w / 12))`와 X축 위치가 엇갈려 "1월"과 "12월" 범례 위에 노드가 없는 것처럼 보이는 어긋남 현상이 발생함.
- **개편 및 동기화**:
  - HTML 이중 막대 및 하단 X축 월 레이블 컨테이너를 12개 동일 비율(`flex: 1`) Grid 컬럼 구조로 변경한다.
  - 모든 막대, 하단 "1월~12월" 텍스트 중심, SVG 꺾은선 노드가 1월부터 12월까지 정확히 **1:1 수직 중앙(`(i + 0.5) / 12 * 100%`)**으로 일치하도록 정밀 교정한다.

### 8) SVG 차트 `preserveAspectRatio="none"` 반응형 100% 1:1 수직 동기화 (2월 쏠림 버그 근본 해결)
- **원인 정밀 진단**:
  - HTML 카드는 12컬럼 Flexbox로 분할되어 있으나, 레이어링된 `<svg>` 요소에 `preserveAspectRatio="none"` 속성이 누락되어 브라우저 가로 폭 및 화면 비율에 따라 SVG 내부 500x195 좌표계가 가로 중앙 정렬(Letterboxing)되면서 SVG 노드 전체 간격이 축소되어 1월 노드가 2월 위치로 밀려나는 현상이 발생했음을 정밀 규명함.
- **근본 해결 방안**:
  - 두 차트의 `<svg>` 요소에 `preserveAspectRatio="none"` 속성을 필수로 부여하여, 어떤 화면 해상도와 카드의 가로 폭에서도 SVG 좌표계가 HTML 12개 월 컬럼 폭(100%)과 **1:1 수직 완벽 동기화(Stretch Alignment)**되도록 조치한다.
  - 1월 선택 시 1월 막대 직상단에 노드 및 선택월 네온 뱃지가 100% 피트되도록 보장한다.

### 9) 차트 시각적 완성도 보강 (노드 원형 비율 보존 & 1월/12월 뱃지 잘림 방지)
- **노드 원형 비율 보존 (Anti-Distortion Circle Scaling)**:
  - SVG 가로 스트레치 조건에서도 노드 원이 타원으로 찌그러지지 않고 **언제나 완벽한 정원(Perfect Circle)** 형태를 유지하도록 디자인 보정을 실시한다.
- **외곽 뱃지 경계 보호 (Edge-Aware Badge Clamping)**:
  - 1월 선택 시 노드 상단 "현재 0.0%" 핑크 네온 뱃지의 좌측 부분이 차트 경계 밖으로 튀어나가 잘리는 현상을 차단하기 위해, 뱃지 렌더링 X좌표 클램핑(`Math.max(45, Math.min(455, x))`) 조치를 적용한다.
  - 1월 및 12월 외곽에서도 뱃지와 텍스트가 잘림 없이 100% 또렷하게 표출된다.

### 10) 애플/스티브 잡스 스타일 프리미엄 차트 개편 (Dynamic Responsive Vector Canvas)
- **SVG 고정 좌표계 및 비율 왜곡 방식 전면 폐기**:
  - `preserveAspectRatio="none"` 또는 고정 viewBox(500x195) 스케일링으로 노드가 타원으로 찌그러지던 이전 처리 방식을 전면 폐기한다.
- **실시간 DOM 컨테이너 픽셀 추적 (Dynamic Container Width Canvas)**:
  - `ResizeObserver` 및 실시간 픽셀 측정(`containerWidth`) 기술을 적용하여 SVG `viewBox`를 실제 카드 가로 픽셀 크기와 100% 동일하게 1:1 렌더링한다.
  - X 좌표 `x = (i + 0.5) * (containerWidth / 12)`를 픽셀 단위로 직접 계산하여, **왜곡 0%의 완벽하고 선명한 정원(Crisp Perfect Circle)** 노드를 보장한다.
- **세계 수준의 핀테크 시각 가시성**:
  - **1월~12월 100% 픽셀 수직 1:1 일치**: 1월 막대/범례 글자 직상단에 노드가 0.0px 오차로 정밀 밀착됨.
  - **외곽 뱃지 클램핑**: 1월 및 12월 선택 시에도 "현재 0.0%" 뱃지가 1px도 잘리지 않고 선명하게 수용됨.
  - **글래스모피즘 & 미세 Glow 광륜**: 애플/레볼루트 수준의 네온 에메랄드 및 분홍 마젠타 펄스 아우라로 전세계 사용자가 감탄할 UI/UX 완성.

### 11) 마우스 호버 툴팁 박스 동적 자동 확장 (Dynamic Auto-Scaling Tooltip Padding)
- **원인 분석**:
  - 10억 원 이상의 고액 자산/부채/순자산 금액 표출 시(`5월 자산: 1,071,739,674원`), 기존 고정 너비(120px) 박스 좌우 테두리를 텍스트가 뚫고 넘어가려 하는 답답함이 유발됨.
- **동적 가변 폭 계산 (Dynamic Auto-Scaling)**:
  - 표출 텍스트 문자열 길이에 따라 박스 너비를 동적으로 자동 계산하는 가변 폭 스케일링 수식(`Math.max(140, text.length * 8.5 + 24)`)을 도입한다.
  - 금액 단위가 백만, 십억, 백억 단위로 커지더라도 좌우 여백(Padding)이 항상 넉넉하게 확보되는 여유로운 글래스모피즘 박스를 제공한다.
  - 가변 폭 동적 확장 시에도 차트 좌우 외곽 테두리 클램핑을 동적 추적하여 경계 밖 튀어나감 현상을 100% 차단한다.

### 12) 좌우 2종 차트 가로 축(X축) 및 바닥선 수직/수평 위치 완벽 정렬 명세 (Horizontal & Vertical Axis Alignment)
- **현상 및 원인 분석**:
  - 좌측 차트(`자산/부채/순자산 추이 Combo Chart`)와 우측 차트(`금융 투자비중 추이 % Chart`)가 나란히 배치될 때, 헤더 영역(범례 높이 차이) 및 카드 내부 패딩/여백 차이로 인해 두 차트의 X축 바닥선(Baseline) 높이가 수평으로 일치하지 않거나, 좌우 카드 간 1월~12월 X축 컬럼 위치 및 좌우 패딩이 달라 가로 축 레이블과 데이터 포인트 위치가 상호 엇갈리는 현상이 발생함.
- **수평/수직 축 정렬 명세 (Dual-Chart Alignment Rule)**:
  1. **X축 가로 위치 정렬 (Horizontal Alignment)**:
     - 좌측 카드와 우측 카드의 내부 패딩(`padding: 24px`), 차트 영역 플롯 좌우 마진 및 12개 월 컬럼의 X좌표 계산 방식(`(i + 0.5) * (width / 12)`)을 100% 동일하게 통일한다.
     - 두 카드가 나란히 보일 때 좌측 1월~12월의 가로 위치와 우측 1월~12월의 가로 위치가 수직선상으로 완벽하게 1:1 정렬된다.
  2. **Y축 바닥선 높이 정렬 (Vertical Baseline Alignment)**:
     - 좌측 카드 헤더 영역(타이틀 + 범례)과 우측 카드 헤더 영역(타이틀 + 뱃지)의 높이(Min-Height 설정)를 맞추어 두 차트 플롯 영역의 X축 바닥선(`baselineY`) Y좌표 높이가 수평으로 100% 일치하도록 보정한다.
  3. **목적**:
     - 2컬럼 레이아웃에서 두 차트를 동시에 비교 감상할 때 시각적 밸런스와 수평/수직 정렬 완성도를 최고 수준으로 유지하기 위함이다.

---

# 17. 대시보드와 보고서

## 17.1 월간 대시보드

필수 표시 항목:

- 조회연월
- 총수입
- 총현금유출
- 실제 소비
- 금융자산 증가
- 부채원금 감소
- 전체 월예산
- 예산 사용액
- 잔여예산
- 월 잉여자금
- 현금흐름 저축률
- 자산형성 포함 저축률
- 마이너스통장 상환액
- 월말 마이너스통장 잔액
- 주택담보대출 원금상환액
- 순자산 증감

### 17.1.1 상단 주요 KPI 카드 최대 4컬럼 제한(Max 4 Columns Cap) 반응형 Media Query Grid 명세

상단 8개 카드는 와이드 화면에서도 한 행에 5~6개로 과도하게 늘어나는 현상을 방지하고 **최대 4컬럼(`repeat(4, 1fr)`)으로 상한선(Cap)을 제한**하되, 브라우저 창 폭이 줄어들면 3개, 2개, 1개로 유연하게 아래로 내리는 **Media Query 기반 반응형 Grid 레이아웃**으로 구성한다:

- **데스크톱 전체화면 (1100px 이상)**: 1행에 최대 4개 제한, 2행에 4개(`4 + 4`)로 완벽한 4x2 대칭 배치 (5개/6개 확장 절대 금지)
- **중간 화면 (801px ~ 1100px)**: 창 너비에 맞춰 3개씩(`3 + 3 + 2`) 자동 하단 배치
- **태블릿 화면 (501px ~ 800px)**: 2개씩(`2 + 2 + 2 + 2`)
- **모바일 화면 (500px 이하)**: 1개씩(`1 x 8`)

```
[데스크톱 넉넉할 때] 최대 4개 제한 4개씩 2줄 (1 2 3 4 / 5 6 7 8) - 5~6개 배치 금지
[창 폭 축소 시] 3개(3+3+2) -> 2개(2+2+2+2) -> 1개(1x8)로 유연한 반응형 조절
```

1. **1번 카드 (당월 총수입)** [유지]:
   - **메인 표시**: `formatKRW(totalIncome)` (월급, 상여, 실비환급, 기타수입 포함 총수입액)
   - **서브 텍스트**: 월급, 상여, 실비환급, 기타수입 포함

2. **2번 카드 (목표 지출 - 월예산)** [유지]:
   - **메인 표시**: `formatKRW(totalBudget)` (16개 카테고리 설정 예산 합계)
   - **서브 텍스트**: 16개 카테고리 설정 예산 합계

3. **3번 카드 (당월 총지출)** [유지]:
   - **메인 표시**: `formatKRW(totalSpent)` (카테고리별 사용합계, 순의료비 반영 실질 지출)
   - **서브 텍스트**: 카테고리별 사용합계 (순의료비 반영)

4. **4번 카드 (잔여 예산)** [수정]:
   - **메인 표시**: `formatKRW(remainingBudget)` + **메인 잔여금액 바로 옆에 `총 사용률 %` (예: 31.8%)를 강조 배지/텍스트로 선명하게배치**
   - **서브 텍스트**: 사용액: `formatKRW(totalSpent)` (총예산 대비)

5. **5번 카드 (고정비 합계)** [개편/추가]:
   - **구성 항목**: 대출, 보험, 통신비, 주거비, 생활고정비, 금융고정비, 교통, 세금 (8개 카테고리)
   - **메인 표시**: `formatKRW(fixedCostsTotal)` (8개 고정비 항목 지출 합계)
   - **서브 텍스트**: 대출, 보험, 통신, 주거, 고정비, 교통, 세금

6. **6번 카드 (실소비 합계)** [위치 변경/개편]:
   - **구성 항목**: 식비, 육아, 의료비(순의료비), 교육비, 기타생활비, 이벤트, 기석용돈, 승주용돈 (8개 카테고리)
   - **메인 표시**: `formatKRW(realConsumptionTotal)` (8개 실소비 항목 지출 합계)
   - **서브 텍스트**: 식비, 육아, 순의료비, 교육, 기타, 이벤트, 용돈2

7. **7번 카드 (실소비 일일 현황)** [신규 추가]:
   - **메인 표시**: 6번 실소비 기준 **'일평균 사용액'** (`realConsumptionTotal / 경과일수`)
   - **서브 텍스트**: '남은 기간 일 권장 사용액' (`(실소비예산 - 실소비합계) / 남은일수`) 및 '남은 D-X일' 표출

8. **8번 카드 (월 잉여자금)** [유지]:
   - **메인 표시**: `formatKRW(monthlySurplus)` (총수입 - 통장 기준 총현금유출)
   - **서브 텍스트**: 총수입 - 총현금유출 (마통 상환 및 저축 여력)

### 17.1.2 월별 예산 수정 및 차월 일괄 이월 적용 명세 [방안 A 확정]

1. **당월 및 미래 월 일괄 적용 및 레코드 동기화 (Forward Propagation & Future Record Sync - 방안 A)**:
   - 특정 선택월(예: 2026-08)에서 카테고리 예산 금액을 수정/저장 또는 [적용]할 경우, 해당 수정값은 **선택된 당월부터 DB 상에 이미 생성되어 존재하는 모든 미래 월(`mKey >= selectedMonth`, 예: 2026-08, 2026-09, 2026-10 ...)의 `monthlyBudgets` 레코드에 일괄 덮어쓰기/동기화**된다.
   - 이미 미래 월에 명시적 예산 레코드가 고정되어 있더라도, 당월 변경 시 해당 미래 월들의 예산 데이터가 함께 갱신되어 미래 월에서 과거 월의 변경사항이 은폐(Shadowing)되는 현상을 완벽히 방지한다.
2. **과거 월 예산 완전 보존 (Past Budget Preservation)**:
   - 선택된 당월 이전의 **과거 월 예산(예: 2026-07 이전 월)은 과거 정해진 기준대로 운영되었으므로 절대 변경되지 않고 100% 보존**된다.
3. **목적**:
   - 당월에 예산을 변경했을 때 새로 변경된 예산 기준이 미래 월까지 실시간으로 일관되게 자동 유지되도록 보장하고, 과거 월의 예산 대비 지출 평가 기록은 훼손되지 않도록 보호하기 위함이다.

### 17.1.3 월별 예산 '적용' 버튼 연동 및 시나리오 (기본안 포함) 수정/저장/삭제 명세 [개편]

1. **'적용' 버튼 명시적 클릭 시 최종 반영 (Manual Apply)**:
   - 화면에서 예산 금액 입력 시 즉시 반영되지 않으며, 사용자가 **`[적용]` 버튼을 클릭해야만 시스템 및 대시보드에 최종 반영**된다.
2. **미래 월 일괄 이월 및 과거 월 보존 (Forward Propagation & History Preservation - 방안 A)**:
   - `[적용]` 클릭 시 수정한 예산은 **해당 월 및 이미 존재하는 모든 미래 월(`mKey >= selectedMonth`)에 일괄 동기화 반영**되며, **과거 월(`mKey < selectedMonth`) 예산은 기존 설정대로 변동 없이 100% 보존**된다.
3. **"기본안" 포함 전 시나리오 자유로운 수정 및 삭제 허용 (Editable & Deletable Base Scenario)**:
   - "기본안"을 포함한 **모든 시나리오를 사용자가 자유롭게 수정, 업데이트 및 삭제**할 수 있다.
   - "기본안" 선택 상태에서도 예산을 수정하여 기존 시나리오 덮어쓰기 저장 및 `[삭제]` 버튼으로 제거가 가능하다.
   - 시나리오 삭제 시 남아있는 다른 시나리오로 자동 전환되며, 전체 삭제 시 기본 템플릿으로 안전하게 재구성된다.
4. **단일 시나리오 드롭다운 통합 관리 (Unified Dynamic Scenario Dropdown)**:
   - "기본안"과 사용자 추가 시나리오를 구별 없이 **동적 1개 드롭다운 목록**으로 통합 관리한다.

### 17.1.4 다년도(Multi-Year) 조회월 및 연도 피벗 컨트롤 명세 [신규 추가 - 옵션 A]

1. **스마트 피벗 연도/월 컨트롤 구조 (Pivot Header Control)**:
   - 상단 헤더의 조회월 컨트롤을 **연도 이동 컨트롤(`[◀ YYYY년 ▶]`)과 월 선택기(`[MM월 ▾]`) 2단계 피벗 구조**로 개편한다.
   - 연도 이동 화살표 버튼(`◀`, `▶`) 클릭 시 1년 단위로 과거/미래 연도로 유연하게 즉시 전환된다.
2. **동적 연도 탐색 범위 (Dynamic Year Range)**:
   - 시스템 현재 날짜(`new Date()`) 기준 **과거 5년 ~ 미래 5년 (총 11개 연도)** 및 DB 내 기록된 모든 거래/스냅샷 연도를 동적으로 자동 집계하여 제공한다.
3. **연간 추이 차트 및 지표 동기화 (Yearly Metrics Sync)**:
   - 선택된 조회 연도가 변경되면 대시보드 및 총 자산 메뉴의 연간 표와 12개월 추이 차트(혼합형/영역형)가 선택된 `YYYY년`의 `01월 ~ 12월` 데이터로 100% 자동 동기화된다.
4. **목적**:
   - 2026년 단일 연도 제한에서 벗어나, 과거 연도 내역 조회부터 차년도 미래 예산 수립까지 영구적으로 가계부 프로그램을 지속 운용하기 위함이다.

## 17.2 카테고리별 현황

각 카테고리마다 다음을 표시한다.

- 월예산
- 사용액
- 잔여예산
- 사용률
- 전월 대비 증감

예산 상태 구분은 다음 정도로 제공할 수 있다.

- 정상
- 주의
- 초과

구체적인 경고 임계값은 구현 전에 확정한다. 과거 제안값인 80%는 아직 필수 요구사항으로 확정하지 않는다.

## 17.3 연간 대시보드

필수 표시 항목:

- 월별 총수입
- 월별 총지출
- 월별 잉여자금
- 월별 순자산
- 월별 총부채
- 월별 주담대 잔액
- 월별 마통 잔액
- 카테고리별 연간지출
- 연간 총수입
- 연간 총지출
- 연간 금융자산 증가
- 연간 부채 감소
- 연간 순자산 증감

### 17.3.1 연간 월별 수입 / 지출 / 잉여자금 흐름 테이블 구성

표 구성 컬럼 (6개 항목):
1. **조회월**: 해당 월 (예: 1월 ~ 12월)
2. **총 수입**: 내역에서 수입 거래 합계 (`totalIncome`)
3. **총 지출**: 내역에서 지출 거래 합계 (`totalExpense`)
4. **실 지출(실비 차감)**: 카테고리별 사용합계금 (또는 `총 지출 - 실비 수입`, 순의료비 반영 실질 지출) (`netExpense`)
5. **월 잉여자금**: `총 수입 - 총 지출` (`monthlySurplus`)
6. **상태**: 흑자 / 적자 구분 배지

## 17.4 차트

차트는 분석에 필요한 범위로 제한한다.

우선순위가 높은 차트:

- 월별 수입·지출 추이
- 월별 순자산 추이
- 월별 주담대 잔액
- 월별 마통 잔액
- 카테고리별 지출 비중
- 실제 소비와 자산형성 비교

---

# 18. 저축률과 자산형성 계산

## 18.1 현금흐름 저축률

```text
현금흐름 저축률
= (총수입 - 총현금유출) ÷ 총수입 × 100
```

## 18.2 자산형성 포함 분석

자산형성액은 다음을 구분해 표시한다.

- 금융자산 증가
- 주택담보대출 원금상환
- 마이너스통장 원금상환

주택담보대출 원금상환은 자산형성 관점에는 포함할 수 있지만 유동성 있는 저축은 아니므로 별도로 표시한다.

## 18.3 세 가지 핵심 금액

프로그램은 매월 다음 세 금액을 구분해야 한다.

1. 실제 생활소비
2. 금융자산 증가
3. 부채원금 감소

이를 합쳐 단순히 모두 `지출`로 보여주지 않는다.

---

# 19. 화면 구성

## 19.1 필수 메뉴

1. 홈
2. 거래내역
3. 월별예산
4. 수입관리
5. 대출관리
6. 자산·부채
7. 월간분석
8. 연간분석
9. 계좌·카드·결제수단
10. 카테고리 및 분류규칙
11. 데이터 가져오기·내보내기
12. 설정

## 19.2 홈 화면 우선 정보

- 이번 달 총수입
- 이번 달 실제 소비
- 이번 달 총현금유출
- 잔여예산
- 월 잉여자금
- 마통 상환액
- 마통 잔액
- 총자산
- 총부채
- 순자산
- 예산 초과 카테고리

## 19.3 UX 원칙

- 한국어 사용
- 원화 표기
- 원 또는 만원 단위 선택 가능
- 모바일과 PC에서 읽기 쉬운 구성 권장
- 표 중심으로 빠르게 수정 가능
- 복잡한 회계용어는 쉬운 표현과 함께 제공
- 색상만으로 상태를 전달하지 않음
- 입력보다 조회·수정이 빠르게 느껴져야 함

반응형 웹앱을 권장하지만, 구현 플랫폼은 사용자의 별도 승인 후 확정한다.

---

# 20. 데이터 내보내기와 백업

## 20.1 내보내기

- 전체 거래내역 Excel 또는 CSV
- 월별 거래내역
- 수입내역
- 월별예산
- 자산·부채 현황
- 대출 상환내역

## 20.2 백업

- 전체 데이터 백업
- 백업파일 복원
- 복원 전 미리보기 또는 확인

Google Sheets 실시간 연동은 향후 검토로 둔다.

---

# 21. 비기능 요구사항

## 21.1 데이터 정확성

- 금액은 원 단위 정수로 처리한다.
- 카드승인과 카드대금 출금 중복 집계를 방지한다.
- 계좌이체는 수입·지출에서 제외한다.
- 거래분할 후 금액 합계는 원거래와 일치해야 한다.
- 업로드 중 오류가 발생하면 전체 취소가 가능해야 한다.

## 21.2 수정 이력

거래 및 주요 자산·부채 데이터는 수정 이력을 남기는 것을 권장한다.

## 21.3 시간 기준

- 대한민국 표준시
- 월별 집계는 거래일 기준

## 21.4 개인정보 보호

실제 계좌번호 전체를 저장할 필요는 없다.

계좌 식별은 사용자가 정한 별칭을 기본으로 한다.

클라우드 서비스로 구현할 경우 인증, 암호화, 접근권한은 별도 기술설계에서 정의한다.

---

# 22. MVP 우선순위

## 22.1 P0: 최초 사용 가능 버전

- 거래내역 등록·수정·삭제
- Excel/CSV 가져오기
- 카테고리 관리
- 월별 예산
- 수입관리
- 월간 대시보드
- 계좌이체 및 카드대금 중복 방지
- 서울페이·선불카드 수동 처리
- 실비 반영 순의료비
- 데이터 내보내기

## 22.2 P1: 자산·부채 버전

- 총자산·총부채·순자산
- 주택담보대출 원금·이자 분리
- 마이너스통장 관리
- 금융자산 증가 분석
- 연간 대시보드
- 예산 시나리오

## 22.3 P2: 운영 편의 개선

다음은 사용자 승인 후 진행한다.

- 반복거래 자동인식
- 규칙 기반 자동분류
- 카드 전월실적 관리
- 부부 개별 로그인
- Google Sheets 연동
- 예산 초과 알림
- 실비 미청구 관리

---

# 23. 필수 인수 테스트

## 테스트 1. 카드 중복 방지

조건:

- 카드로 식비 100,000원 사용
- 다음 달 결제계좌에서 카드대금 100,000원 출금

기대 결과:

- 식비 지출: 100,000원
- 카드대금 출금: 계좌이체
- 총소비: 100,000원

## 테스트 2. 서울페이

조건:

- 475,000원 결제
- 서울페이 잔액 500,000원 충전
- 혜택 25,000원
- 이후 마트에서 100,000원 사용

기대 결과:

- 충전 시 소비 0원
- 기타수입 또는 충전혜택 25,000원
- 서울페이 자산 500,000원 증가
- 사용 시 식비 100,000원
- 사용 후 서울페이 잔액 400,000원

## 테스트 3. 실비

조건:

- 의료비 100,000원
- 실비 70,000원

기대 결과:

- 의료비 총지출: 100,000원
- 실비: 70,000원
- 순의료비: 30,000원

## 테스트 4. 실비가 의료비보다 큰 경우

조건:

- 의료비 50,000원
- 실비 70,000원

기대 결과:

- 순의료비: 0원
- -20,000원으로 표시하지 않음

## 테스트 5. 주담대 원리금 분리

조건:

- 월 납입액 2,700,000원
- 원금 700,000원
- 이자 2,000,000원

기대 결과:

- 현금유출: 2,700,000원
- 대출 예산 사용액: 2,700,000원
- 실제 금융비용: 2,000,000원
- 부채 감소: 700,000원

## 테스트 6. 마통 원금상환

조건:

- 마통 원금 3,000,000원 상환

기대 결과:

- 일반 생활소비에서 제외
- 부채상환 3,000,000원
- 마통 잔액 3,000,000원 감소

## 테스트 7. 청약통장

조건:

- 청약통장 20,000원 납입

기대 결과:

- 현금흐름 예산: 금융고정비 20,000원
- 실제 소비: 0원
- 청약자산: 20,000원 증가

## 테스트 8. 가족 카페

조건:

- 가족이 함께 카페에서 30,000원 사용

기대 결과:

- 식비 30,000원

## 테스트 9. 개인 커피

조건:

- 기석이 혼자 커피 5,000원 사용

기대 결과:

- 기석용돈 5,000원

## 테스트 10. 자녀 의료비

조건:

- 자녀 병원비 50,000원

기대 결과:

- 육아가 아닌 의료비 50,000원

## 테스트 11. 재산세

조건:

- 재산세 400,000원 납부

기대 결과:

- 세금 400,000원
- 주거비에는 미포함

## 테스트 12. 회사 통신비 환급

조건:

- 통신비 100,000원 결제
- 다음 달 회사환급 50,000원 입금

기대 결과:

- 결제월 통신비: 100,000원
- 환급월 기타수입: 50,000원
- 최초 통신비를 50,000원으로 축소하지 않음

## 테스트 13. 혼합 영수증

조건:

- 마트에서 식료품 80,000원, 육아용품 20,000원 구매

기대 결과:

- 기본 분류: 식비 100,000원
- 필요 시 사용자 선택으로 분할 가능

## 테스트 14. 기본 예산 합계

조건:

- 세금 제외 기본예산 사용

기대 결과:

- 월예산 합계: 6,780,000원

## 테스트 15. 보수적 예산 합계

조건:

- 대출예산을 3,150,000원으로 변경

기대 결과:

- 월예산 합계: 7,030,000원

---

# 24. 미확정 사항

구현 전에 사용자 확인이 필요한 항목이다.

1. 프로그램 형태
   - 로컬 PC 프로그램
   - 개인용 웹앱
   - 클라우드 웹서비스

2. 부부 사용 방식
   - 한 계정을 공동 사용
   - 각자 계정으로 동일 가계부 공유

3. 세금 예산 방식
   - 납부월 실적만 관리
   - 월 준비금으로 관리

4. 실비 시차 처리
   - 현재 방식: 수령월 기준, 월 순의료비 최소 0원
   - 대안: 보험금과 원래 의료비 거래를 연결

5. 카드 혜택 및 전월실적 관리 포함 여부

6. 자동분류 기능 수준
   - 없음
   - 단순 키워드 규칙
   - AI 추천

7. Google Sheets 연동 여부

8. 월 마감 기능 필요 여부

9. 부동산 평가액 자동 업데이트 여부

10. 교육비 시작 시점과 확정 예산

---

# 25. 요구사항 검증 결과

## 25.1 누락 점검

다음 대화 요구사항이 본 PRD에 반영되어 있음을 확인했다.

- 뱅크샐러드 거래내역을 주 1회 가져오는 운영방식
- 월별 예산 및 잔여금액 관리
- 부부 수입 구분
- 기석·승주 개인용돈 분리
- 가족 카페와 개인 카페의 분류 차이
- 혼합 마트 영수증의 주목적 분류
- 의료비와 육아비 분리
- 실비 수입 별도 관리 및 순의료비 0원 하한
- 서울페이 충전과 사용 분리
- 스타벅스 선불카드 충전과 사용 분리
- 정부지원 바우처 사용 기록
- 카드승인과 카드대금 출금 중복 방지
- 청약과 자녀투자금의 현금흐름·자산 이중 관점
- 회사 출자금의 자산 처리
- 회사 비용 사후환급의 기타수입 처리
- 재산세의 세금 카테고리 분류
- 주담대 원금·이자 분리
- 마통 이자 및 원금상환 관리
- 월 잉여자금의 마통 상환 연결
- 총자산·부채·순자산 관리
- 기본예산 6,780,000원
- 보수안 7,030,000원
- 교육비 포함 예상안 7,530,000원

## 25.2 충돌 규칙 점검

과거 대화에서 자녀 병원비를 육아로 볼 수 있다는 초기 의견이 있었으나, 이후 의료비 카테고리를 별도로 분리하면서 최신 기준은 다음으로 정리했다.

- 자녀 병원·약국·예방접종·처방약: 의료비
- 자녀 비의료 생활비: 육아

본 PRD는 최신 기준을 반영했다.

## 25.3 추가 내용 점검

다음 내용은 제품 구현상 유용하지만 사용자 확정사항이 아니므로 필수 요구에서 제외하거나 미확정으로 표시했다.

- AI 자동분류
- 카드 전월실적 자동관리
- 부부 다중 로그인
- 알림
- 오픈뱅킹 자동연동
- Google Sheets 실시간 연동
- 월 마감
- 세금 자동계산

## 25.4 금액 검증

- 금융고정비: 20,000 + 20,000 + 190,000 + 190,000 = 420,000원
- 기본예산: 6,780,000원
- 보수안: 6,780,000 + 250,000 = 7,030,000원
- 교육비 포함안: 7,030,000 + 500,000 = 7,530,000원
- 마통 월이자 가정: 90,000,000 × 4.3% ÷ 12 = 322,500원

---

# 26. 개발 AI에 전달할 시작 지시문

아래 문구를 PRD와 함께 개발 AI에 전달한다.

```text
프로젝트 루트의 PRD.md를 먼저 끝까지 읽어 주세요.

PRD의 확정 요구사항과 미확정 사항을 구분하고,
사용자에게 확인되지 않은 기능을 임의로 필수 범위에 추가하지 마세요.

아직 코드를 작성하지 말고 먼저 다음 내용을 제출하세요.

1. PRD 요구사항 요약
2. P0 MVP 구현 범위
3. 데이터 모델
4. 화면 구조
5. 현금흐름·소비·자산이동·부채상환 처리 방식
6. 카드 중복 방지 방식
7. Excel/CSV 가져오기 방식
8. 주요 계산식
9. 인수 테스트 계획
10. 미확정 사항에 대한 질문

사용자가 설계를 승인한 후에만 코딩을 시작하세요.
구현 중 PRD와 다른 결정을 해야 한다면 임의로 변경하지 말고 먼저 승인받으세요.
각 개발 단계가 끝나면 구현 완료, 부분 구현, 미구현 항목을 PRD 번호 기준으로 보고하세요.
```

---

# 27. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| 1.0 | 2026-07-29 | 사용자 대화 내용을 기반으로 최초 작성 |
| 1.1 | 2026-07-31 | 연간 월별 수입/지출/잉여자금 흐름 테이블 컬럼 개편 (조회월, 총 수입, 총 지출, 실 지출(실비 차감), 월 잉여자금, 상태) |
| 1.2 | 2026-07-31 | 대시보드 상단 KPI 카드 구성 변경 (당월 총수입, 목표 지출(월예산), 당월 총지출, 잔여 예산) |
| 1.3 | 2026-07-31 | 계좌 메뉴 -> '총 자산' 개편, 월말 자산 스냅샷 기능, 3개 세부 표 및 합계/투자비중/순자산 수식, 연간 추이 표 & 차트 2개 명세 정의 |
| 1.4 | 2026-07-31 | 월별 자산 데이터 완전 독립 수기 입력 방식 명시 (이전 월 이월 금지) 및 화면 내 '스냅샷' 문구 제거 명세 정의 |
| 1.5 | 2026-07-31 | 선택된 당월 자산 데이터 전체 초기화/삭제 버튼 및 안전 확인 모달 기능 명세 추가 |
| 1.6 | 2026-07-31 | 총 자산 인풋 박스 포커스 시 지우기 동작 없이 즉시 입력 및 플레이스홀더 UX 명세 추가 |
| 1.7 | 2026-07-31 | 총 자산 차트 2종 핀테크 프리미엄 다크 테마 혼합형 차트 & Auto-Scaling 영역형 차트 개편 명세 추가 |
| 1.8 | 2026-07-31 | 차트 요소 마우스 호버 동적 툴팁 인터랙션 및 우측 차트 선택월 네온 노드 하이라이트 강화 명세 추가 |
| 1.9 | 2026-07-31 | 차트 호버 툴팁 위치를 우측 모서리에서 그래프 마우스/요소 근접 스마트 플로팅 위치로 명세 개편 |
| 2.0 | 2026-07-31 | 꺾은선 그래프 노드 X축 1월~12월 범례 1:1 정밀 동기화 및 좁은 간격 어긋남 버그 수정 명세 추가 |
| 2.1 | 2026-07-31 | SVG preserveAspectRatio="none" 속성 추가를 통해 꺾은선 노드 X축 1월~12월 1:1 완전 수직 동기화 및 2월 쏠림 버그 최종 근본 수정 |
| 2.2 | 2026-07-31 | 노드 정원(Perfect Circle) 비율 보존 및 1월 선택월 뱃지 좌측 잘림 방지 클램핑 시각 UX 보강 명세 추가 |
| 3.0 | 2026-07-31 | 애플/스티브 잡스 감성 Dynamic Responsive Vector Canvas 개편으로 왜곡 0% 정원 노드 & 1월~12월 픽셀 1:1 수직 밀착 명세 추가 |
| 3.1 | 2026-07-31 | 호버 툴팁 박스 금액 길이에 따른 동적 가변 폭 자동 확장(Padding) 및 외곽 클램핑 명세 추가 |
| 4.0 | 2026-07-31 | 가계부 대시보드 상단 KPI 카드 8종 Grid 개편 명세 정의 (4번 사용률% 강조, 5번 고정비, 6번 실소비, 7번 실소비 일일현황 신규 추가) |
| 4.1 | 2026-07-31 | 상단 8개 카드 레이아웃을 고정 4개 4개 2줄(gridTemplateColumns: 'repeat(4, 1fr)') 칼대칭 구조로 개편 명세 추가 |
| 4.2 | 2026-07-31 | 상단 8개 카드 레이아웃을 브라우저 창 크기에 따라 4개, 3개, 2개, 1개로 자동 위치 재조정되는 반응형 Auto-Fit Grid 명세 개편 |
| 4.3 | 2026-07-31 | 와이드 모니터에서 5~6개 무제한 확장 방지(Max 4 Columns Cap) 및 반응형 Media Query 4->3->2->1 개편 명세 명시 |
| 5.0 | 2026-07-31 | 월별 예산 수정 시 선택월 및 미래 모든 월에 일괄 이월 적용 및 과거 월 예산 완전 보존 명세 추가 |
| 6.0 | 2026-07-31 | 월별 예산 사용자 정의 프리셋 저장, 불러오기, 삭제 관리 기능 명세 정의 추가 |
| 7.0 | 2026-07-31 | 월별 예산 [적용] 버튼 명시적 클릭 연동 및 시나리오 단일화(기본안)+시나리오 저장/삭제 재정의 명세 추가 |
| 8.0 | 2026-07-31 | "기본안" 포함 모든 시나리오 사용자의 자유로운 수정, 덮어쓰기 및 삭제 허용 명세 개편 추가 |
| 9.0 | 2026-07-31 | 다년도(Multi-Year) 조회월 및 연도 피벗 컨트롤(옵션 A: 연도 선택기 + 월 픽커) 명세 정의 추가 |
| 10.0 | 2026-08-03 | 브라우저 LocalStorage 구조에서 Node.js (Express) + SQLite 기반 백엔드 API & DB 전환 명세 정의 추가 |
| 11.0 | 2026-08-03 | 월별 예산 지출 카테고리 사용자 직접 동적 추가/수정/삭제 CRUD 및 대시보드/내역연동 명세 정의 추가 |

---

# 28. 백엔드 및 데이터베이스 아키텍처 (Node.js Express + SQLite 전환)

## 28.1 목적 및 배경
- **현행**: 브라우저 LocalStorage 기반 데이터 저장 방식 (클라이언트 단독 실행).
- **목표**: 향후 Linux VPS 웹서버 호스팅 및 멀티 디바이스 접속 편의를 위해 `Node.js (Express) + SQLite` 기반 API 백엔드 서버 및 영속성 DB 구조로 이관.

## 28.2 백엔드 서버 명세 (Express.js)
- Express.js 기반 RESTful API 서버 구축 (기본 포트: 3001 또는 Vite 프록시 연동).
- 가계부 거래 내역, 예산 설정, 자산 데이터 CRUD API 구현:
  - `GET /api/transactions` : 거래 내역 목록 및 필터링 조회
  - `POST /api/transactions` : 단일/다중 거래 내역 추가
  - `PUT /api/transactions/:id` : 거래 내역 수정
  - `DELETE /api/transactions/:id` : 거래 내역 삭제
  - `GET/POST /api/budgets` : 예산 및 시나리오 설정 조회/저장
  - `GET/POST /api/assets` : 자산/부채 월별 데이터 조회/저장

## 28.3 데이터베이스 구조 (SQLite)
- 경량형 임베디드 RDBMS인 `better-sqlite3` (또는 `sqlite3`) 활용.
- 데이터베이스 파일 (`data/asset_management.db`) 로컬 및 서버 이관 용이성 보장.
- 스키마 정의:
  - `transactions` 테이블 (id, date, amount, category, subcategory, type, memo, account, payment_method, asset_type, owner, created_at 등)
  - `budgets` 테이블 (year_month, scenario_name, budget_data_json, updated_at 등)
  - `assets` 테이블 (year_month, asset_data_json, updated_at 등)

## 28.4 프론트엔드 연동 및 이관 정책
- 기존 `localStorage` 읽기/쓰기 모듈을 백엔드 REST API 호출 (`fetch` / `axios`) 서비스 레이어로 전면 교체.
- 최초 서버 기동 시 기존 `localStorage`에 데이터가 남아있는 경우, SQLite DB로 1회 자동 마이그레이션(또는 가져오기) 지원.
- Vite 개발 서버에서 Express 백엔드 API 포트(3001)로 proxy 설정하여 CORS 처리 및 로컬 통합 개발 환경 유지.

---

# 29. 월별 예산 지출 카테고리 직접 동적 커스텀 CRUD 명세

## 29.1 목적 및 정책
- 사용자가 본인의 가계 운영 상황에 맞추어 지출 카테고리를 자유롭게 **추가, 수정, 삭제**할 수 있도록 동적 카테고리 관리 기능을 제공함.
- **수입 카테고리 정책**: 수입 항목(기석월급, 승주월급, 기석상여, 승주상여, 실비, 기타수입)은 기존 정책을 유지하며, 사용자 직접 추가/삭제/수정 범위에서 제외함.

## 29.2 세부 요구사항 및 예외 처리 정책
1. **카테고리 구분 그룹 지정 및 대시보드 KPI 5번/6번 동적 집계**:
   - 지출 카테고리 신규 추가 및 수정 시, 해당 카테고리가 **'고정비 그룹'(`isFixed: true`)** 인지 **'실소비 그룹'(`isFixed: false`)** 인지 반드시 선택해야 함.
   - 대시보드의 **5번 KPI 카드(고정비 요약)** 및 **6번 KPI 카드(실소비 요약)** 계산 시 하드코딩된 목록 대신 `isFixed` 속성을 기반으로 새로 추가되거나 변경된 모든 카테고리를 동적으로 100% 자동 합산함.

2. **카테고리 명칭 수정 시 기존 데이터 일괄 갱신**:
   - 카테고리 명칭을 변경(예: `식비` -> `외식/식비`)할 경우, 카테고리 메타 데이터 변경과 함께 기존 거래 내역(`transactions`)의 카테고리 필드 및 월별 예산(`monthlyBudgets`) 키도 일괄 자동 업데이트하여 데이터 무결성을 유지함.

3. **카테고리 삭제 시 기존 거래 내역 안전장치**:
   - 해당 카테고리를 사용하는 기존 거래 내역이 1건 이상 존재하는 경우 **삭제를 차단**하고, *"해당 카테고리를 사용 중인 거래가 N건 있습니다. 거래 내역의 카테고리를 먼저 변경해 주세요."* 안내 경고 모달을 표시함.

4. **신규 카테고리 추가 시 예산 시나리오 이월 정책 (PRD 5.0 준수)**:
   - 과거 월의 예산 기록은 원본 그대로 보존하고, **현재 선택된 월 및 향후 모든 미래 월 예산**에 신규 카테고리와 기본 예산액을 자동 반영함.

5. **시스템 카테고리 자유 수정/삭제 및 방어 조치**:
   - 초기 기본 카테고리(`대출`, `식비`, `교통` 등)를 포함한 모든 카테고리의 자유로운 수정/삭제를 허용하되, 최소 1개 이상의 지출 카테고리가 유지되도록 방어함.

6. **전역 카테고리 하드코딩 제거 및 동적 리팩토링 사양**:
   - **6.1 대시보드 KPI 5번/6번 동적화 ([Dashboard.jsx](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/components/Dashboard.jsx))**: 기존 16개 고정 ID 목록(`fixedCostCatIds`, `realConsumptionCatIds`) 필터링 대신, `c.isFixed === true` (고정비) 및 `c.isFixed === false` (실소비) 속성 기반으로 새로 추가되거나 변경된 모든 커스텀 카테고리를 동적으로 100% 자동 정산함.
   - **6.2 거래 등록 폼 동적 초기값 ([TransactionsView.jsx](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/components/TransactionsView.jsx))**: 수동 거래 등록 폼의 기본 선택값을 고정 문자열(`'식비'`) 대신 `db.categories[0]?.name` (현재 등록된 첫 번째 카테고리)으로 동적 바인딩하여 카테고리 삭제/변경 시 오류 방지.
   - **6.3 영수증 거래 분할 모달 동적 바인딩 ([SplitModal.jsx](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/components/SplitModal.jsx))**: 거래 분할 항목 추가 시 기존 고정 목록 대신 `db.categories` 동적 목록을 참조하여 사용자가 추가한 커스텀 카테고리를 분할 항목으로 즉시 선택 가능하도록 함.
   - **6.4 계산 엔진 동적 순회 ([finance.js](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/utils/finance.js))**: `calculateMonthlyMetrics` 수식이 `DEFAULT_CATEGORIES` 배열 대신 `db.categories` 동적 목록을 참조하여 계산하도록 리팩토링함. (단, 의료비 실비 차감 및 세금 위험 TOP3 제외 특수 규칙은 고유 ID로 안전 보존)
   - **6.5 가져오기 파서 동적 연동 ([ImportModal.jsx](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/components/ImportModal.jsx))**: 엑셀/뱅크샐러드 가져오기 시 매핑 드롭다운에 커스텀 카테고리 목록이 동적으로 반영되도록 처리.

7. **백업 & 복원 자동 호환**:
   - 백엔드 SQLite DB (`settings` 테이블) 및 JSON 백업 파일 (`family_finance_db_v2`)에 사용자 커스텀 카테고리 정보가 포함되어 백업/복구 시 온전함이 유지됨.

8. **예산 시나리오 프리셋 적용 예외 방어 및 다크 테마 디자인 호환**:
   - 저장해둔 예산 시나리오 프리셋을 불러올 때, 카테고리가 삭제되었거나 새로 추가된 상태이더라도 `undefined`/NaN 에러 없이 현재 존재하는 `db.categories` 기준으로 기본 예산액(없으면 0)을 안전 보장함.
   - 신규 추가된 커스텀 카테고리에도 핀테크 프리미엄 다크 테마 배지 및 태그 스타일을 자동 적용함.

9. **월별예산 표 및 모달 UI 명칭 간소화 (반복 단어 '그룹' 제거)**:
   - **테이블 컬럼 헤더 명칭**: 기존 `지출 그룹 (고정비/실소비)`에서 **`지출 구분`**으로 직관적으로 변경.
   - **행 배지 텍스트**: 기존 `🔒 고정비 그룹` / `🛒 실소비 그룹`에서 매 행마다 중복되던 '그룹' 단어를 제거하여 **`🔒 고정비`** 및 **`🛒 실소비`**로 간결하게 표시함.
   - **모달 토글 버튼 텍스트**: 팝업 모달 내부 버튼 텍스트 역시 **`🔒 고정비 (5번 KPI)`** 및 **`🛒 실소비 (6번 KPI)`**로 깔끔하게 다듬어 전체 화면의 시각적 피로도 및 가독성을 대폭 개선함.

10. **카테고리 표준 명칭 '통신' $\rightarrow$ '통신비' 통일 및 자동 마이그레이션**:
    - 기존에 '통신'으로 저장되어 대시보드 카드 예산(140,000원) 연동이 누락되거나 예산이 0원으로 잘못 표시되던 문제를 해결하기 위해, 카테고리 표준 명칭을 **`통신비`**로 정규화함.
    - 데이터 로드 시 기존 DB 카테고리 목록 및 거래 내역의 `통신`을 **`통신비`**로 자동 마이그레이션하여, 대시보드 카드 및 예산/지출 지표에 100% 정상 연동되도록 보장함.


# 30. 총자산 항목(현금/계좌, 투자/자산, 부채) 동적 커스텀 CRUD 명세

## 30.1 목적 및 배경
- 총자산 관리 화면(`AccountsView.jsx`)에서 사용자가 본인의 금융 및 자산 구조 상황(신규 계좌, 투자 항목, 새로운 대출 부채 등)에 맞추어 항목을 자유롭게 **추가, 수정, 삭제**할 수 있도록 동적 자산 관리 기능을 신설함.

## 30.2 3대 자산 그룹별 세부 CRUD 사양
1. **현금/계좌 그룹 (`cashItems`) CRUD**:
   - **추가**: 항목 명칭(예: `카카오뱅크`), 소유자(`기석`/`승주`/`가족공동`), 초기 잔액 입력 후 신규 항목 생성.
   - **수정**: 항목 명칭 및 소유자 정보 수정 지원.
   - **삭제**: 항목 삭제 시 관련 월별 수기 스냅샷 잔액 안전 정제 및 삭제 방어 모달 제공.

2. **투자/자산 그룹 (`investItems`) CRUD**:
   - **추가**: 항목 명칭(예: `삼성증권`, `금투자`), 소유자, 부동산 속성 여부(`isRealEstate`) 선택 생성.
   - **수정**: 항목 명칭, 소유자 및 부동산 여부 속성 수정 가능 (부동산 여부에 따라 투자 비율 및 순자산 계산 자동 동기화).
   - **삭제**: 항목 삭제 및 연동 데이터 안전 정제.

3. **부채 그룹 (`debtItems`) CRUD**:
   - **추가**: 항목 명칭(예: `신용대출`, `자동차할부`), 소유자 지정 신규 항목 생성.
   - **수정**: 항목 명칭 및 소유자 변경 지원.
   - **삭제**: 항목 삭제 및 연동 데이터 안전 정제.

## 30.3 계산 엔진 및 저장소 동기화 정책
1. **자산 지표 동적 집계**:
   - `getAssetMetrics` 집계 수식이 하드코딩된 목록 대신 동적 자산 구조(`db.assetStructure`)를 참조하여 총자산, 현금성자산, 투자자산, 부채, 순자산을 100% 자동 합산 정산함.
2. **영속성 및 백업 동기화**:
   - 사용자 정의 자산 항목 데이터는 SQLite 백엔드 DB (`settings` 테이블 `assetStructure` 키) 및 LocalStorage, JSON 전체 백업 파일에 자동 동기화되어 백업/복구 시 온전함이 유지됨.

## 30.4 전수 검사 기반 하드코딩 제거 및 리팩토링 상세 사양
1. **자산 항목 상수 고정 탈피 (`finance.js`)**:
   - 기존 `CASH_ASSET_ITEMS`, `INVEST_ASSET_ITEMS`, `DEBT_ITEMS` 고정 배열 상수를 초기 시드(Seed) 데이터 용도로만 사용하도록 변경.
   - 전역 상태 DB(`db.assetStructure`)에 3대 자산 항목 구조를 관리하고, 사용자가 신규 추가/수정/삭제 시 DB에 동적으로 영구 저장함.
2. **자산 집계 수식 동적화 (`AppContext.jsx`)**:
   - `getAssetMetrics` 수식이 정적 상수 배열 대신 `db.assetStructure` (또는 동적 자산 목록)을 직접 순회하여 합산하도록 리팩토링함.
   - 부동산 자산 차감 수식도 특정 ID(`inv_realestate`) 하드코딩 대신 속성 플래그 (`c.isRealEstate === true`)를 탐지하여 동적으로 차감 계산함.
3. **총자산 화면 UI 동적 바인딩 (`AccountsView.jsx`)**:
   - `AccountsView.jsx`에서 `CASH_ASSET_ITEMS` 등 정적 상수를 import하여 테이블을 렌더링하던 방식을 제거하고, `useApp()`을 통해 전달받은 동적 자산 목록(`assetStructure`)으로 전면 바인딩하여 렌더링함.
4. **저장소 및 백업 연동 (`storage.js`)**:
   - `loadDatabase()`, `loadDatabaseAsync()`, `saveDatabase()`가 사용자 정의 자산 구조(`assetStructure`)를 포함하여 저장 및 복원하도록 처리함.

## 30.5 세부 놓치기 쉬운 고려사항 및 예외 처리 방어 규칙 (Checklist)
1. **신규 항목 추가 시 초기 잔액 입력 옵션 (선택 사항, 기본값 0원)**:
   - 신규 자산/부채 항목 생성 시 잔액 입력은 선택 사항이며, 비워두거나 `0`으로 두면 `0원`으로 안전 생성됨. 만약 초기 금액(예: 500만원)을 입력한 경우 당월 스냅샷 잔액에 자동 초기 할당되어 수기 재입력 불필요.
2. **소유자(`owner`) 지정 및 렌더링 유지**:
   - 자산 항목 신규 등록/수정 시 소유자(`기석`, `승주`, `가족공동`)를 반드시 지정할 수 있도록 Form 필드를 제공하고, 표에서 소유자 배지가 동적 렌더링되도록 함.
3. **투자/자산 항목 '부동산' 속성 체크박스 (`isRealEstate`)**:
   - 금융 투자 비율 수식(`(총투자 - 부동산) / 총자산 * 100`)을 위해 투자/자산 추가/수정 시 *"부동산 자산 여부 (금융투자 비율 계산 제외)"* 체크박스 선택권을 보장함.
4. **항목 삭제 시 12개월 월별 스냅샷 데이터 안전 청소 (Cleanup)**:
   - 자산 항목 삭제 시 `db.monthlyAssetSnapshots`의 전 월(1월~12월) 기록에서 고아 데이터(Orphan Key)가 남지 않도록 동시 청소하여 NaN이나 잔여 메모리 낭비를 방지함.
5. **거래 입력 폼 계좌 드롭다운 동적 연동 (`TransactionsView.jsx`)**:
   - 사용자가 신규 현금/계좌 항목을 추가/수정/삭제하면, 거래 입력 폼의 결제 계좌 선택 드롭다운 목록에도 즉시 동적으로 반영되도록 연동함.
6. **12개월 연간 자산 트렌드 차트 (`yearlyAssetMetrics`) 동적 재계산**:
   - 항목 추가/수정/삭제 즉시 연간 12개월 자산 차트 그래프가 깨짐 없이 실시간 재계산 및 렌더링되도록 보장함.
7. **백업 JSON 복원 및 초기화 호환성**:
   - JSON 파일 백업/복원 시 `assetStructure`가 누락된 구버전 백업 파일이더라도 기본 시드 데이터를 자동 보장하는 하위 호환성(Backward Compatibility) 탑재.
8. **자산 항목 수정 모달 내 당월 현재 잔액 자동 표출 및 수정 연동**:
   - 기존 항목의 `[✏️ 수정]` 버튼 클릭 시, 수정 팝업 모달 잔액 입력 필드에 당월 현재 입력되어 있는 잔액(`snap[groupKey]?.[item.id]`)이 자동으로 불러와서 표출됨.
   - 모달에서 명칭/소유자뿐만 아니라 현재 잔액을 수정하고 `[수정 완료]`를 누르면 당월 자산 스냅샷 잔액도 즉시 갱신 반영됨.


# 31. 총자산 화면 몰입형 세그먼트 탭(Segmented Tab Focus) UI/UX 개편 명세

## 31.1 목적 및 배경
- 기존 3컬럼 가로 가두기 방식(`minmax(360px, 1fr)`)으로 인한 숫자 절단 현상(`1376...`), 내부 횡스크롤바 발생, 및 부채 컬럼의 수직 비대칭 공백 문제를 근본적으로 해결함.
- 스티브 잡스 철학 기반의 **몰입형 세그먼트 탭 (Segmented Tab Focus) UX**를 도입하여 100% Full-Width 화면에서 시원하고 직관적인 자산 관리 경험을 제공함.

## 31.2 세부 UI/UX 구성 사양
1. **상단 고정 4대 마스터 자산 KPI 카드**:
   - 화면 최상단에 `총 자산`, `현금성 자산`, `투자 자산`, `부채 / 순자산` 4대 마스터 카드를 고정 배치하여, 어떤 탭을 선택하든 전체 자산의 거시적 맥락(Master View)을 항상 보장함.

2. **세그먼트 탭 컨트롤 (Segmented Tab Control)**:
   - 마스터 카드 하단에 핀테크 스타일의 100% 풀-위드 세그먼트 탭 바 배치:
     - `[ 🏦 현금·계좌 (N개) ]`
     - `[ 📈 투자·자산 (N개) ]`
     - `[ 💳 부채 (N개) ]`
     - `[ 📊 전체 한눈에 보기 ]`

3. **100% Full-Width 데이터 테이블 뷰 (탭별 몰입)**:
   - **현금·계좌 탭**: 현금 계좌 항목만을 100% 너비의 시원한 프리미엄 데이터 테이블로 표출 (`계좌명`, `소유자 배지`, `잔액 입력 필드`, `수정/삭제 작업`).
   - **투자·자산 탭**: 투자 및 자산 항목 100% 너비 표출 (`자산명/부동산 태그`, `소유자`, `평가액 입력`, `수정/삭제`). 하단에 `투자 합계`, `자산 합계`, `투자 비중 %` 통합 집계 바 표출.
   - **부채 탭**: 부채 항목 100% 너비 표출 (`부채 항목명`, `귀속`, `잔액 입력`, `수정/삭제`). 하단에 `부채 합계`, `순자산 합계` 집계 바 표출.
   - **전체 한눈에 보기 탭**: 3개 카드를 세로 100% 너비 스택(Vertical Full-Width Stack)으로 펼쳐서 전 자산을 한 화면에서 세로 스크롤로 한눈에 조망하는 뷰 제공.

4. **사용자 경험(UX) 개선 이점**:
   - 숫자가 절단되거나 횡스크롤바가 일절 발생하지 않음.
   - 각 계좌/자산 항목의 이름, 소유자 배지, 잔액 입력 칸, 수정/삭제 버튼이 시원하고 완벽하게 정렬됨.
   - 데이터 개수 불균형(현금 14개, 부채 2개)에 따른 화면 빈 공간(Dead Space) 완벽 소멸.


# 32. 전월 대비 순자산 증감률(MoM % & 금액) 지표 표출 명세

## 32.1 목적 및 배경
- 사용자가 선택한 당월의 순자산이 직전 월(전월) 대비 얼마나 상승/하강하였는지 증감률(%)과 차액(원)을 직관적으로 확인하고 시각적 자산 성장의 쾌감을 전달함.
- 상단 순자산 마스터 카드와 하단 연간 순자산 꺾은선 차트(Chart 1)의 쌍방향 통합으로 거시적 성장 맥락을 강화함.

## 32.2 연산 수식 (Finance Math)
- `prevYearMonth`: 선택월(`selectedMonth`)의 직전 월 (예: `2026-07` $\rightarrow$ `2026-06`)
- `currentNetAsset`: 당월 순자산 (`assetTotal - debtTotal`)
- `prevNetAsset`: 전월 순자산 (`prevAssetTotal - prevDebtTotal`)
- `momDiff`: `currentNetAsset - prevNetAsset` (전월 대비 차액)
- `momRate`: `prevNetAsset > 0 ? (momDiff / prevNetAsset) * 100 : 0` (전월 대비 증감률 %)

## 32.3 세부 UI/UX 구성 사양
1. **상단 순자산 마스터 카드 내 트렌드 뱃지 (Smart Trend Pill Badge)**:
   - 최상단 `순자산` 카드 내부 금액 표출 부근에 네온 글래스 트렌드 뱃지 통합:
     - **상승 시**: `🟢 ▲ +X.X% (전월 대비 +OOO원)` (에메랄드 그라데이션)
     - **하강 시**: `🔴 ▼ -X.X% (전월 대비 -OOO원)` (로즈 그라데이션)
     - **보합 시**: `▬ 0.0% (전월 동일)` (그레이 배지)
     - **전월 기록 없는 경우**: `(기준월)` 안내 라벨

2. **연간 순자산/총자산 꺾은선 차트 (Chart 1) 내 MoM Delta 태그**:
   - 선택월 상단 네온 핀 뱃지: `현재 순자산 OO원 (▲ X.X%)`
   - 차트 마우스 호버 툴팁: `월 순자산: OO원 (전월 대비 OO원, ▲ X.X%)`


# 33. 설정 & 백업 화면 "데모 데이터 초기화" 버튼 명칭 변경 명세

## 33.1 목적 및 배경
- 설정 & 백업 화면(`SettingsView.jsx`)에 위치한 전체 DB 초기화 기능 버튼의 기존 명칭인 `"데모 데이터 초기화"`를 유저 실데이터 관리에 부합하는 명확하고 직관적인 **`"데이터 초기화"`**로 개편함.

## 33.2 세부 문구 변경 사양
1. **버튼 명칭 변경**:
   - 기존: `🔄 데모 데이터 초기화` $\rightarrow$ 변경: **`🔄 데이터 초기화`**
2. **모달 팝업 및 설명 문구 표준화**:
   - 설정 카드 카드 설명: *"데이터베이스를 초기화하고 기본 카테고리 및 설정 상태로 리셋합니다."*
   - 확인 모달 타이틀: **`데이터 초기화`**
   - 확인 모달 실행 버튼: **`초기화 실행`**


# 34. 상단 네비게이션 "내역&가져오기" -> "거래내역" 메뉴 명칭 변경 명세

## 34.1 목적 및 배경
- 상단 글로벌 네비게이션 탭 메뉴의 기존 명칭인 `"내역&가져오기"`를 보다 직관적이고 보편적인 **`"거래내역"`**으로 개편하여 유저 접근성과 가독성을 향상함.

## 34.2 세부 문구 변경 사양
1. **상단 네비게이션 바 메뉴 (`Navbar.jsx`)**:
   - 기존: `📝 내역&가져오기` $\rightarrow$ 변경: **`📝 거래내역`**
2. **거래 내역 화면 헤더 타이틀 (`TransactionsView.jsx`)**:
   - 기존 유지: `📋 {selectedMonth} 거래 내역 (총 {length}건)` (변경 없음)


# 35. 초기 기본 조회월(Default Selected Month) 동적 현재년월 설정 명세

## 35.1 목적 및 배경
- 앱 접속 시 사용자가 별도 조회월을 변경하지 않더라도 **오늘 날짜가 속한 현재 년월(YYYY-MM)**을 자동으로 기본 조회월로 지정하여 실시간 가계부 관리 유용성을 극대화함.

## 35.2 세부 기능 사양
1. **초기 상태 지정 함수 (`AppContext.jsx`)**:
   - `getCurrentYearMonth()` 함수 신설:
     ```javascript
     const getCurrentYearMonth = () => {
       const now = new Date();
       const year = now.getFullYear();
       const month = String(now.getMonth() + 1).padStart(2, '0');
       return `${year}-${month}`;
     };
     ```
   - `selectedMonth` 초기 `useState` 값으로 `getCurrentYearMonth()` 반환값 적용.
2. **글로벌 폴백 처리 (`Navbar.jsx` 등)**:
   - `selectedMonth || getCurrentYearMonth()` 폴백 연동으로 언제나 오늘 날짜 년월 기본 바인딩 보장.


# 36. 상단 브랜드 타이틀 "P0 MVP" 문구 삭제 명세

## 36.1 목적 및 배경
- 상단 글로벌 헤더 바(`Navbar.jsx`)의 브랜드 타이틀에 남아있던 초기 임시 라벨인 `"P0 MVP"` 텍스트를 제거하고, 정식 프로덕션 수준의 깔끔한 브랜드 타이틀인 **`"4인 가족 통합 자산관리"`**로 개편함.

## 36.2 세부 문구 변경 사양
- **상단 헤더 타이틀 (`Navbar.jsx`)**:
  - 기존: `4인 가족 통합 자산관리 P0 MVP` $\rightarrow$ 변경: **`4인 가족 통합 자산관리`**


# 37. 전 화면 금액 인풋 박스 천단위(1,000원) 콤마(`,`) 자동 포맷팅 명세

## 37.1 목적 및 배경
- 금액 입력 시 0의 개수를 하나하나 눈으로 세는 번거로움을 완전히 해소하기 위해, 모든 금액 인풋 박스에 숫자를 입력하는 즉시 **천단위 콤마(`,`)가 자동으로 실시간 형성**되는 포맷팅 규칙을 전수 적용함.

## 37.2 전수 조사 대상 인풋 박스 목록 (Total 8개 포인트)
1. **총자산 화면 (`AccountsView.jsx`)**:
   - 현금/계좌 테이블 잔액 인풋 박스 (`cash` group)
   - 투자/자산 테이블 잔액 인풋 박스 (`invest` group)
   - 부채 테이블 잔액 인풋 박스 (`debt` group)
   - 자산 항목 추가/수정 모달 잔액 인풋 박스 (`assetForm.defaultBalance`)
2. **월별 예산 화면 (`BudgetView.jsx`)**:
   - 카테고리별 예산 금액 입력 인풋 박스 (`currentBudgets[catName]`)
   - 시나리오 프리셋 커스텀 예산 입력 인풋 박스 (`customPresets[presetKey].budgets[catName]`)
3. **거래 내역 화면 (`TransactionsView.jsx`)**:
   - 수기 거래 추가/수정 모달 금액 입력 인풋 박스 (`formTx.amount`)
4. **거래 분할 모달 (`SplitModal.jsx`)**:
   - 거래 분할 금액 인풋 박스 (`amount1`, `amount2`)

## 37.3 세부 기술 사양
- **포맷팅 헬퍼 함수 (`src/utils/finance.js` 또는 `storage.js`)**:
  - `formatNumberWithCommas(val)`: 숫자를 콤마 포맷 문자열로 변환 (예: `1000000` $\rightarrow$ `"1,000,000"`)
  - `parseCommaNumber(formattedStr)`: 콤마가 포함된 문자열에서 순수 숫자만 추출하여 `Number` 타입 반환
- **인풋속성 변경**: `type="text"` & `inputMode="numeric"`으로 설정하여 천단위 콤마 입력과 모바일 숫자 키패드를 동시 지원.


# 38. 대시보드 화면 "카테고리별 예산 현황" 항목 수 동적 바인딩 명세

## 38.1 목적 및 배경
- 대시보드 화면(`Dashboard.jsx`) 카드 제목에 하드코딩 되어있던 `"16개 항목"` 텍스트를 실시간 DB 카테고리 개수로 동적 연동하여 카테고리 신규 추가/삭제 시 유연하게 자동 동기화되도록 개선함.

## 38.2 세부 변경 사양
- **대상 파일**: [src/components/Dashboard.jsx](file:///c:/Users/gsk12/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/%EC%9E%90%EC%82%B0%EA%B4%80%EB%A6%AC%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%A8/src/components/Dashboard.jsx)
- **변경 사항**:
  - 기존: `📊 카테고리별 예산 현황 (16개 항목)`
  - 변경: **`📊 카테고리별 예산 현황 ({(db.categories || DEFAULT_CATEGORIES).length}개 항목)`**


# 39. 잠재적 오류 유발 항목 전수조사 결과 및 시스템 개선 명세 (구현 대기)

## 39.1 목적 및 배경
- `finance.js` 하드코딩 및 Supabase DB간의 ID/키 미스매치와 같은 전반적인 시스템 리스크 항목을 전수조사하여, 데이터 유실 및 결함을 사전에 방지하고 아키텍처의 유연성과 안정성을 극대화하기 위한 상세 사양을 정의함.

## 39.2 전수조사 결과 및 세부 개선 사양 (Total 6개 주요 영역)

### 1) 수입 카테고리(Income Categories) 동적 관리 및 소유자 기반 집계/상세 뷰 명세
- **배경**: 현재 `기석월급`, `기석상여`, `승주월급`, `승주상여`, `실비`, `기타수입` 6개 수입 카테고리가 코드 상수로 고정되어 있어 신규 수입 항목(예: `부업`, `배당금`, `연말정산환급금`, `임대수입`) 추가 및 저장이 불가하고, 카테고리 추가 시 표가 옆으로 끝없이 넓어지는 가로 팽창(Horizontal Table Blowup) UI 결함이 존재함.
- **수입 카테고리 데이터 구조 (`incomeCategories`)**:
  - 각 수입 카테고리는 다음 필드를 필수 보유: `{ id, name, owner }`
  - `owner` (소유자 귀속): `'기석'` | `'승주'` | `'가족공동'` (신규 카테고리 생성 시 소유자 지정을 필수로 선택)
- **수입 카테고리 CRUD 관리 UI**:
  - **위치**: 수입 관리 화면(`IncomeView.jsx`) 상단 우측에 `+ 수입 항목 추가/관리` 버튼 배치.
  - **전용 관리 모달**: 카테고리 명칭(Name) 및 소유자 지정(`기석`/`승주`/`가족공동`)을 설정/수정/삭제 가능한 모달 신설.
- **연간 월별 수입 표 UI/UX 명세 (가로 팽창 방지 + 소유자/카테고리 상세 드릴다운)**:
  - **기본 컴팩트 요약 뷰 (Compact 5-Column Table)**:
    - 기본 컬럼 구조: `월` | `기석 수입 합계` | `승주 수입 합계` | `기타/공동 수입` | `총 수입` (총 5개 컬럼 고정으로 수입 항목이 10~20개로 늘어나도 가로 스크롤 팽창 100% 방지)
  - **소유자별 세부 항목 드릴다운 (Drill-down Popover / Accordion)**:
    - 각 월의 '기석 수입' 또는 '승주 수입' 셀을 클릭/호버 시 해당 월에 발생한 세부 카테고리별 내역(예: 기석 월급: 700만, 기석 상여: 200만, 기석 부업: 50만 등)이 세로 카드/팝오버 형태로 깔끔하게 표출.
  - **상세 조망 뷰 스위처 (View Switcher Tabs)**:
    - 표 상단에 `[전체 요약]`, `[기석 수입 상세]`, `[승주 수입 상세]`, `[카테고리 전체 펼침]` 뷰 전환 탭을 제공하여, 사용자가 원하는 소유자의 수입 내역만 선택적으로 돋보기 뷰로 집중 조망 가능.

### 2) 카테고리 고유 ID(`cat_id`) 기반 릴레이션 매핑 개편 사양
- **배경**: `transactions`(거래 내역) 및 `monthly_budgets`(월별 예산) 데이터가 카테고리 ID가 아닌 카테고리 이름 문자열(`'식비'`)로 연결되어 있어, 카테고리 명칭 변경 시 기존 예산/거래와의 매핑이 끊기거나 0원으로 붕 뜨는 결함이 존재함.
- **거래 내역 데이터 구조 및 뱅크샐러드 가져오기 파이프라인 (사용자 부담 0%)**:
  - 뱅크샐러드 원본 엑셀 파일은 예전 그대로 아무런 가공 없이 업로드(사용자 작업 0%).
  - 파일 업로드 시 앱이 카테고리 맵을 자동 연동하여 거래 데이터 객체에 ID와 이름을 동시 보장 저장: `{ id, date, amount, category_id: 'cat_food', category: '식비', ... }`
- **ID 기반 금액 연산 엔진 (`calculateMonthlyMetrics` 등)**:
  - 모든 금액 집계, 예산 남은 금액, 수입/지출 연산 엔진이 `tx.category_id === cat.id`를 기준으로 동작.
  - 카테고리 명칭을 `'식비'` $\rightarrow$ `'식대/외식'`으로 변경하더라도 `cat.id`(`cat_food`)는 고정되므로 과거/현재/미래 거래 내역 및 예산 연동이 단 1원도 잘리지 않고 100% 보존.
- **데이터 무손실 안전 마이그레이션 4단계 절차**:
  - **1단계 (자동 사전 백업)**: 마이그레이션 직전 Supabase DB 전체 타임스탬프 백업 생성.
  - **2단계 (하이브리드 읽기 보장)**: 코드에서 `category_id`와 `category` 이름을 동시 지원하여 연산 오작동 차단.
  - **3단계 (DB 이관 스크립트)**: `Name -> ID` 변환표를 기반으로 DB 내 `monthly_budgets` 키 및 `transactions` 컬럼을 안전하게 이관.
  - **4단계 (0원 오차 검증)**: 마이그레이션 전/후 월별 합계 금액 대조로 0원 오차 검증.

### 3) 의료비 / 실비 특수 환급 연산 로직의 고유 ID(`id`) 매핑 사양
- **배경**: 기존 연산이 문자열 이름(`'의료비'`, `'실비'`)으로 지정되어 카테고리 명칭 변경 시 계산이 오작동하는 문제.
- **개선 사양**:
  - 불필요한 별도 boolean 플래그 추가 없이, 고유 ID(`cat_med`: 의료비 지출, `inc_med_ref`: 실비 환급 수입)를 기반으로 직접 연산하도록 매핑하여 명칭이 수정되어도 안전하게 순의료비(`netMedicalExpense = MAX(0, medicalExpense - medicalRefund)`)가 집계되도록 단순화.

### 4) 브라우저 이탈 시 300ms 디바운스 데이터 유실 방지 (BeforeUnload Flush) 사양
- **배경**: `saveDatabase` 300ms 디바운스 지연 중 브라우저 탭을 닫거나 새로고침(F5) 시 마지막 변경사항 요청이 취소되는 문제.
- **개선 사양**:
  - 브라우저 `window.addEventListener('beforeunload', ...)` 이벤트 발생 시 펜딩(Pending) 중인 디바운스 동기화를 즉시 플러시(Flush Execution)하여 최후의 변경사항이 100% Supabase DB에 저장되도록 보장.

### 5) 예산 시나리오(Budget Presets) CRUD 및 활성 시나리오 영구 보존 사양
- **배경**: 신규 시나리오를 생성/저장한 후 새로고침(F5) 시 드롭다운이 항상 `기본안`으로 리셋되거나 저장된 시나리오가 지워지는 결함 존재.
- **원인 분석 및 개선 사양**:
  - `storage.js` 160번 라인의 잘못된 조건문(`> 0` $\rightarrow$ `=== 0`)을 수정하여, 유저가 새로 만든 시나리오 목록(`customBudgetPresets`)이 새로고침 시 삭제 및 초기화되는 현상을 100% 방지.
  - `activeScenario` 선택 상태를 `db.activeScenario` 속성에 바인딩하여 Supabase `settings` 테이블(`key: 'activeScenario'`)에 저장/로드되도록 연결.
  - 시나리오 조회, 생성, 수정, 삭제(CRUD) 파이프라인 완성.

### 6) 카테고리 고정비/실소비(`isFixed`) DB 영구 보존 및 하드코딩 상성 병합 오버라이드 제거 사양
- **배경**: 카테고리 편집 모달에서 고정비/실소비(`isFixed`)를 변경(예: 세금을 고정비로 변경)한 후 새로고침 시, DB 설정값이 유지되지 않고 하드코딩 초기값으로 원복되는 결함 존재.
- **원인 분석 및 개선 사양**:
  - `storage.js`의 `loadDatabaseAsync()`가 새로고침 시 DB에서 가져온 `categories` 설정값보다 소스코드 상수 `DEFAULT_CATEGORIES`를 우선시하여 덮어쓰던 병합 오버라이드 결함 제거.
  - Supabase `settings` 테이블의 `categories` JSON 구조 내 `isFixed` 값이 새로고침 시에도 100% 우선 존중되도록 복원 로직 보완.

## 39.3 구현 대기 상태 정의
- 본 사양은 전수조사 및 오류 원인 분석 결과로 정리 완료되었으며, 현재는 **`구현 대기 (Pending Implementation)`** 상태로 유지함. 사용자의 다음 구현 착수 지시 시 순차적으로 반영을 시작함.


# 40. 다중 계정(1 Account = 1 Household Space) 로그인 인증 및 전역 가족 구성원(Owner) 동적 연동 명세 (확정 요구사항)

## 40.1 목적 및 배경
- 기존에 '승주 & 기석' 가계부로 하드코딩되어 있던 시스템을 **다중 계정(다중 사용자 가계부) 및 부부 중심 자산관리 프로그램**으로 확장함.
- 복잡한 초대/그룹 매핑 구조 대신 **"1개의 계정(ID/PW) = 1개의 독립된 가계부 공간"**으로 설계하여 부부가 하나의 계정을 공유하거나 개인별 독립 가계부를 사용하는 직관적인 도메인 모델을 구축함.
- 계정 내에서 가족 구성원(소유자, Owner)을 동적으로 직접 관리하고, 애플리케이션 전체 화면(수입, 자산, 거래내역, 대시보드 등)에 동적 연동함.

## 40.2 세부 기능 사양

### 1) 계정 도메인 및 DB 데이터 격리 (Multi-tenancy)
- **`users` (또는 `accounts`) 테이블 신설**:
  - `id`, `username` (또는 이메일), `password_hash`, `household_name` (가계부 명칭), `created_at`
- **DB 컬럼 확장 및 데이터 격리**:
  - 기존 모든 테이블(`transactions`, `monthly_budgets`, `monthly_assets`, `custom_budget_presets`, `settings`)에 `user_id` 컬럼 필수 추가 및 인덱스 생성.
  - 모든 백엔드 API (`GET /api/db`, `POST /api/transactions` 등)에서 로그인된 계정의 `user_id` 조건(`WHERE user_id = ?`)을 필수로 적용하여 타 계정 데이터 유출 차단.

### 2) 사용자 인증 및 세션 보안 (Auth System)
- **회원가입 기능 (`AuthView.jsx`)**:
  - 필수 입력값: 아이디(ID), 비밀번호(PW), 비밀번호 확인, **가계부 명칭** (예: *"기석 & 승주 가계부"*), **초기 가족 구성원** (예: *"기석"*, *"승주"*)
- **인증 및 세션 유지**:
  - `bcrypt` 단방향 암호화 비밀번호 저장.
  - JWT 토큰 / 세션 기반 인증 및 브라우저 재접속 시 자동 로그인 유지.
  - 상단 네비게이션 바(`Navbar.jsx`)에 사용자 프로필 및 `[로그아웃]` 버튼 제공 (로그아웃 시 전역 상태 및 로컬 캐시 초기화).
- **Auth Gate (인증 게이트웨이)**:
  - 최상위 앱(`App.jsx`)에서 로그인 상태 판단 후 미로그인 시 `AuthView.jsx` (로그인/회원가입) 렌더링, 로그인 성공 시 메인 가계부 서비스 화면 렌더링.

### 3) 가족 구성원(소유자, Owner) 동적 관리 사양
- **설정 탭(`SettingsView.jsx`) 내 [가족 구성원 관리] 섹션**:
  - 구성원 추가, 삭제, 이름 변경, 배지 대표 색상(파란색, 분홍색 등) 지정 기능 제공.
- **`settings` 저장 구조**:
  - `family_members` JSON 배열로 관리 (예: `[{ id: 'm1', name: '기석', color: '#3B82F6' }, { id: 'm2', name: '승주', color: '#EC4899' }, { id: 'm3', name: '가족공동', color: '#10B981' }]`).

### 4) 엑셀 업로드 및 소유자(`owner`) 원클릭 일괄 매핑 사양
- **거래 내역 소유자(`owner`) 필드 유지**: 개인별 지출 분석 및 내역 필터링 기능 유지.
- **엑셀 업로드 모달(`ImportModal.jsx`) 원클릭 지정 UX**:
  - 파일 드롭존 상단에 **[소유자 선택] 세그먼트 버튼 (예: 🔵 기석 | 🔴 승주 | 🟢 가족공동)** 제공.
  - 소유자 선택 후 엑셀 파일 선택 시 파일 안의 수백 개 거래 내역 전체에 선택한 `owner`가 **자동 일괄 매핑**.
  - *(스마트 자동감지)* 업로드 파일명에 '기석' 또는 '승주'가 포함된 경우 해당 소유자 버튼 선제적 자동 활성화.

### 5) 전역 화면 소유자(Owner) 및 가계부 명칭 동적 연동 사양
1. **`Navbar.jsx` (상단 헤더)**:
   - 하드코딩된 타이틀을 로그인 계정의 가계부 명칭(예: *"OO & OO 가계부"*)으로 동적 변경, 우상단 프로필 및 로그아웃 버튼 노출.
2. **`TransactionsView.jsx` (거래 내역)**:
   - 소유자 필터 탭(`[전체 / 🔵 기석 / 🔴 승주 / 🟢 가족공동]`) 및 거래 등록/수정 모달의 소유자 선택 옵션을 설정된 동적 구성원 목록과 연동.
3. **`ImportModal.jsx` (엑셀 업로드)**:
   - 엑셀 업로드 소유자 지정 버튼을 동적 구성원 목록과 연동.
4. **`IncomeView.jsx` (수입 관리)**:
   - 소유자별 수입 요약 카드, 소유자별 상세 뷰 스위처 탭(*"기석 상세"*, *"승주 상세"* 등), 수입 카테고리 모달 소유자 옵션을 동적 연동.
5. **`AccountsView.jsx` (총 자산)**:
   - 계좌/부채 등록 모달 소유자 옵션, 계좌 목록 배지(Badge) 텍스트 및 지정 색상, 소유자별 자산 요약 카드 및 비중 차트를 동적 연동.
6. **`Dashboard.jsx` (대시보드 메인)**:
   - 상단 브랜딩 텍스트 및 소유자별 지출/수입 지표 연동.
7. **`SettingsView.jsx` (설정)**:
   - [가족 구성원 관리] 카드 신설 및 데이터 편집 기능 제공.

## 40.3 기존 데이터 마이그레이션 대상 계정 명세
- **초기 생성 및 데이터 이관 대상 계정 정보**:
  - **아이디 (`username`)**: `togom`
  - **비밀번호 (`password`)**: `1122` (운영 마이그레이션 시 암호화 저장)
  - **가계부 명칭 (`household_name`)**: `기석 & 승주 가족 가계부`
  - **초기 가족 구성원 (`family_members`)**: `['기석', '승주', '가족공동']` (마이그레이션 대상 `togom` 계정 전용)
- **이관 처리**:
  - DB 구조 변경(컬럼 추가) 후 기존에 수집되어 있던 모든 거래, 예산, 자산 데이터의 `user_id`를 위 `togom` 계정 고유 ID로 일괄 자동 할당함.

## 40.4 인증 화면 UX 개선 및 신규 계정 가족 구성원 데이터 바인딩 명세 (1, 2, 5번 요구사항)

1. **로그인 화면 예시 문구 정제 (1번)**:
   - 로그인 폼(`AuthView.jsx`)의 아이디 입력 필드 `placeholder`에서 특정 사용자명인 `togom` 표기를 삭제하고 `"아이디를 입력하세요"`로 정제.
2. **회원가입 화면 범용 가족 구성원 예시 적용 (2번)**:
   - 회원가입 폼의 초기 가족 구성원 입력 안내 예시 문구 및 state 초기값을 기존 특정 사용자 이름(`기석, 승주, 가족공동`) 대신 범용 예시인 `남편, 아내, 가족공동`으로 변경.
3. **신규 가입 시 가족 구성원 저장 및 fallback 기본값 격리 (5번)**:
   - 회원가입 시 사용자가 입력한 `initial_members`가 신규 계정의 `settings` (`familyMembers`)에 정상 저장되도록 구성.
   - 입력값이 없거나 fallback 동작 시 기존 하드코딩된 `기석, 승주` 대신 `남편, 아내, 가족공동`이 기본값으로 사용되도록 처리하여 불필요한 마이그레이션용 이름 노출 방지.

## 40.5 다중 계정 데이터 격리를 위한 DB 복합 기본키(Composite PK) 전환 명세 (3, 5번 근본 원인 해결)

1. **배경 및 원인 분석**:
   - 기존 `settings`, `monthly_budgets`, `monthly_assets`, `custom_budget_presets` 테이블의 기본키(PK)가 단일 컬럼(`key`, `year_month`, `id`)으로 지정되어 있어, 타 계정이 동일한 키값을 저장할 때 `duplicate key value violates unique constraint` 오류 발생.
   - 이로 인해 신규 회원가입 시 에러가 표출되며, 가족 구성원(`familyMembers`) 저장이 실패하여 로그인 시 설정값이 노출되지 않는 문제 원천 차단.

2. **DB 테이블 복합 기본키(Composite Primary Key) 변경 명세**:
   - **`transactions` 테이블**: `PRIMARY KEY (user_id, id)` 복합키 전환 및 `user_id` 인덱스 생성 (거래 ID 유니크 격리 및 검색 속도 향상).
   - **`settings` 테이블**: `PRIMARY KEY (user_id, key)` 복합키 전환.
   - **`monthly_budgets` 테이블**: `PRIMARY KEY (user_id, year_month)` 복합키 전환.
   - **`monthly_assets` 테이블**: `PRIMARY KEY (user_id, year_month)` 복합키 전환.
   - **`custom_budget_presets` 테이블**: `PRIMARY KEY (user_id, id)` 복합키 전환.

3. **자동 DDL 마이그레이션 & 쿼리 처리 명세**:
   - 백엔드DB 초기화 시 기존 단일 기본키 제약조건(`settings_pkey`, `monthly_budgets_pkey` 등)을 제거(`DROP CONSTRAINT IF EXISTS`)하고 계정별 복합 기본키를 자동 재설정.
   - `createUser` 및 `syncFullDatabase` 실행 시 `ON CONFLICT (user_id, key) DO UPDATE ...` 구문을 적용하여 계정 간 데이터 격리 및 저장 보장.

## 40.6 백엔드 DB 원자성 및 트랜잭션 보장 명세 (Strict Transaction Policy)

1. **원칙 및 요구사항 (ACID 원자성 준수)**:
   - 두 개 이상의 DML(CUD) 구문을 실행하는 모든 백엔드 처리 로직(회원가입, 전체 동기화 등)은 **반드시 DB 트랜잭션(`BEGIN` ~ `COMMIT` / `ROLLBACK`)으로 래핑**해야 함.
   - 후속 작업 중 단 하나라도 실패 시 `ROLLBACK`을 수행하여 불완전한 부분 레코드(Partial Record)가 DB에 잔존하지 않도록 원천 차단함.

2. **적용 및 준수 대상**:
   - **회원가입 (`createUser`)**: `users` 계정 생성과 `settings` 초기 설정 저장을 명시적 `BEGIN` ~ `COMMIT` / `ROLLBACK` 트랜잭션으로 원자적(Atomic) 처리.
   - **데이터 일괄 동기화 (`syncFullDatabase`)**: 거래내역, 월별 예산, 월별 자산, 프리셋, 설정 저장을 명시적 트랜잭션으로 동기화.
   - **향후 모든 데이터베이스 다중 테이블 조작 및 CUD 처리**: 트랜잭션 래핑 필수 준수.

## 40.7 수입관리 화면 가족 구성원별 상세 탭 동적 렌더링 명세

1. **배경 및 원인 분석**:
   - 수입 관리 화면(`IncomeView.jsx`)에서 가족 구성원 상세 탭 클릭 시 탭 상태값(`viewTab`)은 구성원 ID로 저장되나, 표 렌더링 조건문이 하드코딩 문자열(`giseok`, `seungju`)을 검사하여 상세 수입 데이터 셀(`<td>`)이 무효화되고 빈 행만 표출되는 현상 개선.

2. **세부 기능 사양**:
   - **탭-상태값 동적 매핑**: 가족 구성원 목록(`familyMembers`)의 각 구성원에 대해 `viewTab`을 구성원 ID(`m.id`)로 설정.
   - **표 렌더링 조건식 동적화**: `<thead>` 및 `<tbody>` 내 조건문을 하드코딩 대신 `familyMembers`에서 선택된 구성원에 매핑하여, 해당 소유자(`cat.owner === selectedMember.name`) 수입 카테고리 컬럼 및 금액 합계가 연간 집계표에 동적으로 정밀 표출되도록 구성.

---

# 41. 신규 계정 로그인 및 데이터 동적화 작업 명세

## 41.1 개요 및 목적
- **목적**: 기존 시스템 전반에 남아있던 특정 계정(`togom`) 귀속 데이터(기석/승주 개인용돈, 개인계좌, 부동산 명칭 등) 하드코딩 및 프론트엔드 fallback 누출 문제를 원천 제거하고, **모든 화면의 데이터를 현재 로그인한 계정(`user_id`)의 DB 저장값 기준으로 100% 동적 조회·생성**하도록 개편함.
- **핵심 원칙**:
  1. 신규 가입 계정은 이전 계정의 데이터나 개인적인 카테고리/자산 명칭이 일절 유출·공유되지 않음.
  2. 신규 계정의 카테고리 및 초기 월별 예산은 회원가입 시 사용자가 직접 정의함.
  3. 신규 계정의 총자산은 완전한 빈 화면(Empty State)으로 시작하며, 사용자가 직접 등록한 항목과 월별 스냅샷 잔액으로만 관리됨.

## 41.2 회원가입 프로세스 연동 및 카테고리/예산 초기화 명세
1. **회원가입 UI (다단계 Wizard 구조 도입)**:
   - **Step 1 (기본 정보)**: 아이디(`username`), 비밀번호(`password`), 가계부 명칭(`householdName`), 가족 구성원(`initialMembers`).
   - **Step 2 (수입 카테고리 구성)**: 가구 구성원에 맞춘 수입 항목(월급, 상여 등)과 공통 수입 항목(기타수입 등)을 사용자가 직접 확인/수정/추가.
   - **Step 3 (지출 카테고리 및 월별 예산 입력)**: 필수 생활 지출 및 변동 지출 카테고리 목록과 각 카테고리별 **월별 목표 예산(원)**을 사용자가 직접 입력.
2. **백엔드 DB 초기화 트랜잭션 (`createUser`)**:
   - 회원가입 API(`/api/auth/register`) 수신 시 `users`, `settings`(`familyMembers`, `categories`, `incomeCategories`, `assetStructure`, `activeScenario`), `custom_budget_presets`, `monthly_budgets`를 단일 트랜잭션(`BEGIN` ~ `COMMIT`)으로 원자적 저장.
   - `assetStructure`는 반드시 **`{ cashItems: [], investItems: [], debtItems: [] }` (빈 배열)**로 초기화하여 기존 자산 데이터 유입 차단.
   - 입력된 카테고리별 예산으로 첫 프리셋(`basic`, '기본안')과 당월 `monthly_budgets` 레코드를 자동 생성.

## 41.3 화면별 카테고리 동적 바인딩 명세
1. **대시보드 (`Dashboard.jsx`)**:
   - 고정비/실소비/전체 예산 집계 시 하드코딩된 카테고리 수(예: "16개...") 및 하드코딩된 예시 텍스트("식비, 육아, 순의료비...")를 제거하고, 현재 사용자의 `db.categories` 기반으로 동적 렌더링.
2. **거래내역 (`TransactionsView.jsx`)**:
   - 거래 등록 모달 및 카테고리 필터 셀렉트 박스에서 레거시 정적 상수(`DEFAULT_CATEGORIES`) 참조를 완전히 제거하고, `db.categories` 목록과 동적 매핑.
   - 기본 선택 카테고리를 하드코딩된 문자열 대신 현재 사용자 카테고리의 첫 번째 항목으로 안전하게 폴백.
3. **월별예산 (`BudgetView.jsx`)**:
   - 프리셋 목록 및 카테고리 예산 행을 로그인된 계정의 `db.categories` 및 `db.customBudgetPresets` 기준으로 렌더링.
   - 하드코딩된 "기본안 (688만원)" 등의 정적 텍스트를 제거하고 실제 사용자의 프리셋 금액으로 동적 계산.
4. **수입관리 (`IncomeView.jsx`)**:
   - 레거시 상수(`DEFAULT_INCOME_CATEGORIES`) 폴백을 제거하고, `db.incomeCategories` 및 가입 시 등록된 `familyMembers` 기준으로 소유자별 수입 표 및 탭을 동적 바인딩.

## 41.4 총자산 화면 및 자산 관리 구조 개편 명세
1. **최초 상태 (Empty State) 보장**:
   - 신규 가입 계정은 현금 계좌, 투자 자산, 대출 부채 항목이 일절 존재하지 않는 **완전한 빈 상태(Empty State)**로 진입.
   - 레거시 계정의 자산 항목(`길음뉴타운 6단지`, `종암 SK`, `토스증권(기석)` 등)이 폴백으로 노출되는 현상 원천 제거.
   - 각 탭(현금, 투자, 부채) 및 전체 탭에서 항목이 0개일 때 친절한 안내 문구와 함께 `[항목 추가]` 유도 UI 제공.
2. **사용자 직접 등록 및 월별 스냅샷 관리**:
   - 사용자가 직접 명칭, 소유자(가족 구성원 선택), 초기 잔액, 자산 성격(부동산 여부 등)을 입력하여 자산 항목을 생성.
   - 자산 항목 구조(`assetStructure`)는 마스터 메타데이터로 관리되고, 매월 말일 시점별 잔액은 `monthlyAssetSnapshots[yearMonth]`에 독립적으로 기록·보존되는 스냅샷 아키텍처 확립.
   - 상단 KPI 카드의 세부 설명("주담대 + 마통 잔액" 등)도 등록된 항목명을 기반으로 동적 표출.

## 41.5 대시보드 8대 핵심 KPI 카드 균등 너비 그리드 및 모바일 친화 텍스트 줄바꿈 명세
1. **문제점 분석**:
   - 고정비/실소비 카테고리 개수가 많아지거나 명칭이 길어질 경우, CSS Grid의 기본 `1fr`(`minmax(auto, 1fr)`) 특성으로 인해 카드의 `min-content` 크기에 따라 4개 컬럼의 가로폭이 불균등하게 늘어나거나 찌그러지는 현상 발생.
2. **모바일 호환 및 레이아웃 제약 사항**:
   - **말줄임표(`text-overflow: ellipsis`) 사용 금지**: 모바일 환경에서는 마우스 호버(`title` 툴팁)를 통한 상세 확인이 불가능하므로, 텍스트가 잘리지 않고 모든 카테고리 명칭이 온전히 노출되어야 함.
   - **자연스러운 다중 라인 줄바꿈 (`word-break: keep-all; overflow-wrap: break-word`)**: 카테고리가 많아지더라도 텍스트가 카드 폭 내에서 자연스럽게 여러 줄로 줄바꿈되어 모바일과 데스크톱 모두에서 한눈에 전체 항목을 열람할 수 있도록 지원.
3. **CSS Grid 완전 균등 분할 아키텍처**:
   - **컬럼 최소 너비 제약 (`minmax(0, 1fr)`)**: `.kpi-grid-container`의 컬럼 템플릿을 `repeat(4, minmax(0, 1fr))`로 강제하고, 모든 자식 카드에 `min-width: 0`을 지정하여 내부 텍스트 줄 수나 길이에 전혀 구애받지 않고 4개 카드의 가로폭이 항상 25% 완전 균등하게 유지되도록 고정.
   - **반응형 미디어 쿼리 균등 분할**: 태블릿(1100px 이하 3열, 800px 이하 2열) 및 모바일(500px 이하 1열) 환경에서도 `minmax(0, 1fr)`을 적용하여 동일한 비율로 깔끔하게 정렬.
   - **동일 행 카드 높이 동기화**: Grid의 `align-self: stretch`와 카드의 `display: flex; flex-direction: column; justify-content: space-between;` 구조를 활용하여, 한 카드의 줄 수가 늘어나 행 높이가 확장되더라도 같은 행의 모든 카드가 균일한 높이와 정렬을 유지하도록 구현.

## 41.6 개발자 개인정보 및 하드코딩된 식별자 전면 제거 및 일반화/동적화 명세
1. **문제점 분석**:
   - 코드베이스 전반에 개발자 및 특정 가족 구성원의 실명(`기석`, `승주`, `서아`, `서빈`), 사적 계좌/자산 명칭(`길음뉴타운 6단지`, `종암 SK`, `토스증권(기석)` 등), 특정 병원 명칭(`성북우리아이들병원`) 및 아이디(`togom`) 등이 설명 텍스트, placeholder, 기본 상수(fallback)로 하드코딩되어 있어 개인정보 유출 및 타 사용자 이용 시 부적절한 노출 문제 발생.
2. **화면별 수정 명세**:
   - **수입관리 화면 (`IncomeView.jsx`)**:
     - 상단 헤더 설명문구 `"기석/승주/공동 소유자별..."` -> 현재 로그인 계정에 등록된 `familyMembers` 목록을 기반으로 `${familyMembers.map(m => m.name).join('/')} 소유자별 수입 항목을 동적으로 관리하고...`로 100% 동적 치환. 등록된 구성원이 없을 시 `'가족 구성원별'`로 폴백.
     - 수입 추가 모달 placeholder: `"예: 기석부업, 배당금, 연말정산환급금"` -> `"예: 부업, 배당금, 연말정산환급금"`으로 일반화.
     - 탭 및 모달 소유자 폴백: `기석`, `승주` 하드코딩을 제거하고 `familyMembers`가 없을 시 일반 명칭(`남편`, `아내`, `가족공동`)으로 폴백.
   - **설정&백업 화면 (`SettingsView.jsx`)**:
     - 가족 구성원 추가 input placeholder: `"새 구성원 이름 (예: 서아, 서빈)"` -> `"새 구성원 이름 (예: 첫째, 둘째 또는 자녀)"`로 일반화.
   - **상단 내비게이션 바 (`Navbar.jsx`)**:
     - 가계부 타이틀 기본 폴백: `'기석 & 승주 가족 가계부'` -> `'우리 가족 가계부'`로 일반화.
     - 사용자명 기본 폴백: `'togom'` -> `'사용자'`로 일반화.
   - **거래내역 화면 (`TransactionsView.jsx`)**:
     - 거래 등록 모달 placeholder: `"예: 성북우리아이들병원 또는 쿠팡 장보기"` -> `"예: 마트 장보기, 병원 진료 등"`으로 일반화.
     - 가족 구성원 셀렉트 박스 폴백: `기석`, `승주` 제거 후 일반 명칭(`남편`, `아내`, `가족공동`)으로 대체.
   - **엑셀 가져오기 모달 (`ImportModal.jsx`)**:
     - 기본 선택 소유자: `'기석'` 고정값 제거, `familyMembers[0]?.name || '가족공동'`으로 동적 바인딩.
     - 소유자 목록 폴백: `기석`, `승주` 대신 일반 명칭(`남편`, `아내`, `가족공동`) 사용.
   - **총자산 화면 (`AccountsView.jsx`)**:
     - 자산 등록 모달 소유자 목록 폴백: `기석`, `승주` 대신 일반 명칭(`남편`, `아내`, `가족공동`) 사용.
   - **레거시 상수 및 유틸리티 (`finance.js`, `storage.js`)**:
     - `DEFAULT_CATEGORIES`: `'기석용돈'`, `'승주용돈'` -> `'남편용돈'`, `'아내용돈'`.
     - `DEFAULT_INCOME_CATEGORIES`: `'기석월급'`, `'기석상여'`, `'승주월급'`, `'승주상여'` -> `'남편월급'`, `'남편상여'`, `'아내월급'`, `'아내상여'`.
     - `CASH_ASSET_ITEMS`, `INVEST_ASSET_ITEMS`: 개인 실명 계좌/자산 명칭(`기업은행(기석)`, `토스증권(기석)`, `길음뉴타운 6단지`, `종암 SK` 등)을 `'급여통장(남편)'`, `'급여통장(아내)'`, `'비상금통장'`, `'주택청약'`, `'국내주식/ETF'`, `'부동산'` 등 일반 금융 자산 예시로 전면 치환.
   - **백엔드 DB 초기화 로직 (`server/db.js`)**:
     - 기본 가계부 생성 명칭: `'기석 & 승주 가족 가계부'` -> `'우리 가족 가계부'`.
     - 기본 `familyMembers` 초기값: `기석`, `승주` -> `남편`, `아내`.
